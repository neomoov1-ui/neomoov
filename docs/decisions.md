# Journal des décisions techniques

Une entrée par décision : date, contexte, décision, conséquences. Le cahier des charges fait foi ; ce journal couvre ce sur quoi il est muet.

| Date | Décision | Contexte et conséquences |
|---|---|---|
| 2026-09-22 | Dépôt créé, étape 0 | Cahier des charges v1.0, 18 prompts et design (lot 1, 20 maquettes) copiés dans `docs/`. Docker, pnpm et l'accès GitHub restent à installer sur le poste avant l'étape 1 |
| 2026-09-22 | Taxes des chauffeurs : pas de perception ni de remise en leur nom | Décision du fondateur (D26 du document de référence), conforme à la section 5.8 : les taxes sur les tarifs sont reversées au chauffeur, qui déclare lui-même. À faire confirmer par un comptable |
| 2026-09-22 | Dénomination légale | « GROUPE NOUVEAU SYSTEME KARDINAL (GROUPE NSK) INC. » pour Stripe, les magasins et les documents juridiques (D25) |
| 2026-09-22 | Poste de développement | pnpm 12.5.1 installé. Poste à 6 Go de mémoire vive, WSL absent : choix à faire entre Docker Desktop et une base de développement hébergée avant l’étape 1 |
| 2026-09-22 | Monorepo amorcé avant l’étape 1 complète | pnpm 12.5.1, Turborepo, TypeScript strict, `packages/config` et `packages/domain`. Les applications (API, worker, web, mobiles) restent à créer à l’étape 1, qui attend le choix entre Docker et une base hébergée |
| 2026-09-22 | Moteur de tarification écrit en avance (étape 4, partie pure) | `packages/domain/src/pricing`, 70 tests, couverture de 100 %. Aucune valeur métier dans le code : les règles sont des entrées |
| 2026-09-22 | Flex et Priorité ne se cumulent pas | Le cahier des charges est muet. Les deux options se contredisent (attendre jusqu’à 15 minutes, ou le chauffeur le plus proche tout de suite). Erreur `FLEX_AND_PRIORITY_EXCLUSIVE`. À confirmer par le fondateur |
| 2026-09-22 | Heures de pointe : donnée de configuration, plages provisoires | La section 5.1 ne les définit pas. Elles viennent de `settings` (`peakWindows`). Proposition provisoire, acceptée « on y reviendra » par le fondateur : du lundi au vendredi de 6 h 30 à 9 h 30 et de 15 h 30 à 18 h 30, plus les vendredis et samedis de 22 h à 2 h. Elles ne servent qu’à refuser l’Offre Flex, jamais à majorer un prix |
| 2026-09-22 | Forfait : options et suppléments ignorés, et signalés | Le forfait remplace tout le calcul (section 5.1). Le devis renvoie `ignoredOptions`. Un forfait dont le total ne se décompose pas en lignes arrondies est refusé (`FLAT_RATE_NOT_DECOMPOSABLE`) : My Hub devra valider le montant à la saisie |
| 2026-09-22 | Course offerte : Neomoov absorbe aussi les frais et les taxes | Confirmé par le fondateur. Une course « offerte » coûte 0,00 $ au client : le drapeau `waivesFees` est vrai pour les promotions « troisième course offerte » et « dixième course offerte » dans les données de départ. Le chauffeur reçoit son tarif normal en compensation |
| 2026-09-22 | Prix maximal consenti = prix affiché + marge de 20,00 $ au plus | Confirmé par le fondateur. « Plafond 20 $ » du prompt 04 : marge d’attente et d’arrêts (`maxExtraAllowanceCents`), l’attente facturée par minute entière après 5 minutes gratuites, jamais au-delà |
| 2026-09-22 | Ordre des opérations | Minimum, puis suppléments fixes, puis multiplicateur Flex ou Priorité, puis chauffeur favori, comme l’ordre des lignes de la section 5.1 |
| 2026-09-22 | Moteur de règlement écrit en avance (étape 9, partie pure) | `packages/domain/src/settlement` : `classifyRideForStatement`, `packBillingLines`, `buildStatement`, `periodForGeneration`, `evaluateSuspension`. Jeu de 200 courses mixtes recalculé indépendamment dans le test |
| 2026-09-22 | Taxes du chauffeur quand une promotion réduit le prix payé | Le chauffeur reçoit les taxes calculées sur son tarif complet (compensation comprise), puisqu’il les remet lui-même (D26) ; Neomoov absorbe l’écart avec les taxes réellement perçues du client. Sur une course offerte à 0,00 $, il reçoit donc son tarif et ses taxes en entier. **À faire confirmer par le comptable** avec la question D2 (traitement fiscal de la compensation et de la redevance) |
| 2026-09-22 | Période du relevé en dates locales | Du lundi au dimanche en dates de Montréal, sans arithmétique de fuseau : une course appartient au relevé de sa date locale. Générée le vendredi, la période est la dernière semaine entièrement écoulée |
| 2026-09-22 | Base de développement hébergée (option B) plutôt que Docker sur le poste | Décision du fondateur : le poste n’a que 6 Go de mémoire vive et WSL n’est pas installé. PostgreSQL et PostGIS chez Supabase, région `ca-central-1` (Canada central), compte Neomoov ; Redis chez Railway (aucune région canadienne chez Railway ni Upstash, acceptable pour la V1 : la migration canadienne avant le lancement commercial est déjà prévue, section 10.3). Docker reste utilisé seulement dans l’intégration continue GitHub. L’étape 1 est adaptée : `pnpm db:up` devient facultatif, `.env` porte `DATABASE_URL` et `REDIS_URL` hébergées. Connexion à Supabase par le « session pooler » (IPv4), jamais la connexion directe (IPv6 seulement) |
| 2026-09-22 | Schéma de données dans `packages/db` plutôt que dans `apps/api/src/db` | L'API et le worker partagent le même schéma : un paquet évite un import croisé entre applications. Drizzle ORM 0.45, `postgres` 3.4, migrations dans `packages/db/drizzle`, scripts `pnpm db:generate`, `db:migrate`, `db:rollback`, `db:reset`, `db:seed` à la racine. Le prompt 02 s'applique avec ce chemin |
| 2026-09-22 | Migrations inverses écrites à la main | drizzle-kit ne génère pas de migration inverse : chaque `drizzle/NNNN_nom.sql` a son inverse dans `drizzle/down/NNNN_nom.sql` ; `db:rollback` l'exécute et retire l'entrée du journal `drizzle.__drizzle_migrations`. Un test vérifie que chaque migration a son inverse |
| 2026-09-22 | Types PostGIS : correction du SQL généré | drizzle-kit met les types paramétrés entre guillemets (`"geography(point,4326)"`), ce que PostgreSQL refuse. `scripts/fix-migration.js` retire ces guillemets après chaque génération (chaîné dans `db:generate`). Colonnes : `geography(point,4326)` pour les positions, `geography(polygon,4326)` pour les zones, `geography(linestring,4326)` pour les traces, index GiST |
| 2026-09-22 | `driver_locations` partitionnée par jour, `driver_presence` pour la position courante | La table de Drizzle est remplacée en migration 0001 par une table partitionnée (`ensure_driver_locations_partition`, `purge_driver_locations` à 90 jours). La recherche « chauffeurs dans un rayon » ne lit pas l'historique : elle lit `driver_presence` (une ligne par chauffeur en ligne, index GiST) via la fonction `drivers_within(lng, lat, rayon, catégorie)` |
| 2026-09-22 | Numérotation sans trou par table de compteurs | Les séquences PostgreSQL laissent des trous en cas d'annulation de transaction. Table `counters` (portée, dernière valeur) avec `next_counter(portée)` transactionnel : `next_invoice_number()` (NM-0000001), `next_invoice_supplier_sequence(chauffeur)` (séquence par fournisseur, exigée pour la facturation), `next_ride_public_number(fuseau)` (NM-AAAA-MM-JJ-0001), `next_driver_public_number()` (CH-00001) |
| 2026-09-22 | État de course `expired` ajouté | La section 5.2 mentionne l'« expiration » de l'état `quoted` sans état cible. `expired` est l'état terminal d'un devis jamais confirmé ; il n'apparaît dans aucune liste client. Machine à états déclarée dans `packages/domain/src/rides/state-machine.ts` avec gardes nommées (`vehicle_category_at_least_reserved`, `waited_five_minutes_and_two_contacts`) et effets déclarés, sans logique d'infrastructure |
| 2026-09-22 | Tables en ajout seul par déclencheur | `audit_log` et `ride_events` refusent UPDATE et DELETE (`forbid_change()`, code `insufficient_privilege`). Les purges de rétention (section 5.15) devront passer par un rôle qui désactive le déclencheur, jamais par l'API |
| 2026-09-22 | Réglages en base, lus à chaque calcul | `settings` (clé, portée, valeur JSON) porte tous les nombres d'exploitation (frais 200, redevance 90, TPS 50 000 ppm, TVQ 99 750 ppm, annulation 120 s et 500, non-présentation 700, attente 300 s et 50 par minute, rayons 2, 5, 10 km puis zone, vagues 20 s, offres 15 s, seuil de solde 15 000, impayé 7 jours, heures de pointe D29). `buildPricingRules` (`packages/db`) assemble les `PricingRules` du moteur à partir des tables ; un test vérifie que les données de départ redonnent l'exemple de contrôle (24,55 $ et 31,56 $) |
| 2026-09-23 | Retours du fondateur : décisions D31 à D47 du document de référence v1.1 | Cahier des charges v1.1 copié dans `docs/`, amendements aux prompts dans `docs/prompts/18-amendements-v1-1.md` (à lire avec chaque prompt). Points qui touchent le code : préavis de 2 heures (`rides.min_lead_seconds` 7200, drapeau `FEATURE_IMMEDIATE_RIDES` désactivé, erreur `LEAD_TIME_TOO_SHORT`), péages et trafic à l'heure prévue dans le devis, `benchmarkCheck` contre `competitor_benchmarks`, négociation bornée à 70 % avec contre-offre au-dessus du prix affiché derrière `FEATURE_NEGOTIATION_ABOVE_MAX` (avis juridique en attente), deux choix de paiement (`prepaid`, `pay_driver_after`) et adaptateur `InstallmentProvider` (plus de 150 $), préférences étendues, `client_driver_links` (« Mes chauffeurs », « Mes clients »), `organizations` et `organization_id` dès le schéma, entretien vocal des candidats (V1.1), enregistrement audio et vidéo (V2), photos réelles uniquement dans les écrans (`docs/design/credits-photos-site.json` pour les crédits du site). La cinquième entité s'appelle provisoirement Kardinal Labs (D31) |
| 2026-09-23 | Réglages de départ à ajouter au prochain passage sur `packages/db` | `rides.min_lead_seconds` 7200, `pricing.negotiation_floor_ppm` 700000, `pricing.negotiation_ceiling_ppm` 1300000, `pricing.benchmark_margin_ppm` 50000, `payments.installment_min_cents` 15000 ; drapeaux `FEATURE_IMMEDIATE_RIDES`, `FEATURE_NEGOTIATION_ABOVE_MAX`, `FEATURE_INSTALLMENTS`, `FEATURE_RIDE_SERIES` désactivés. Le réglage `rides.scheduled_min_lead_seconds` (1800) devient le préavis des courses immédiates quand le drapeau sera activé |
| 2026-09-24 | Serveurs chez LWS (D48) : VPS KVM, Docker Compose, Caddy ; base Supabase Canada central ; Railway et Vercel en secours seulement | Décision du fondateur. Sections 3.5, 10.1 à 10.3 du cahier des charges v1.1 et amendements des prompts 01 et 16. Tous les centres de données LWS sont en France : la base et les documents restent au Canada, la résidence des serveurs applicatifs est une décision à prendre avec l'avocat avant le lancement commercial (conserver LWS avec l'évaluation Loi 25, ou rapatrier au Canada ; `compose.prod.yml` doit fonctionner à l'identique sur les deux) |
| 2026-09-24 | Base de développement Supabase `neomoov-dev` migrée et semée | Migrations 0000 et 0001 appliquées (72 tables, PostGIS 3.3.7, pgcrypto), données de départ créées. Les scripts de `packages/db` chargent le `.env` de la racine (`src/env.ts`) et exigent TLS hors localhost. `TEST_DATABASE_URL` reste vide : les tests d'intégration attendront un projet de test distinct |
| 2026-09-24 | Étape 1 : versions et outils | Versions épinglées : NestJS 12.1, Next.js 16.3 (App Router, Tailwind 4.3, TanStack Query 5, zustand 5), Expo SDK 57 (React Native 0.86, React 19.2, Expo Router 57, Reanimated 4), i18next 26 et react-i18next 17, Drizzle 0.45, vitest 5, TypeScript 7.0.2, Turborepo 2.11, pnpm 12.5.1, Node 24. pnpm en disposition isolée (défaut) : le réglage `node-linker=hoisted` écrit la veille n'était pas en vigueur (`node_modules/.modules.yaml` : `isolated`) alors que tout passait, il est retiré pour que l'intégration continue reproduise exactement le poste ; à revoir seulement si une compilation Expo échoue sur une double copie de react. Scripts d'installation autorisés dans `pnpm-workspace.yaml` (`allowBuilds`) ; NestJS 12 et dotenv 18 exclus de la politique d'âge minimal. Les tests NestJS passent par le greffon swc de vitest (métadonnées de décorateurs), la construction par tsc. `pnpm dev` lance API, worker et web ; les mobiles par `pnpm dev:client` et `pnpm dev:driver` |
| 2026-09-24 | Files et cache sans Redis | Sans `REDIS_URL`, l'API et le worker exécutent les tâches en mémoire, immédiatement et sans persistance, avec un avertissement au démarrage ; `GET /v1/health` rapporte `redis: not_configured` et `queues.mode: memory`. En production, `REDIS_URL` est obligatoire (refus au démarrage). Redis tournera sur le VPS LWS |
| 2026-09-24 | Valeur vide = variable absente | Un `.env` copié de `.env.example` contient `CLE=` sans valeur ; Zod refusait alors les énumérations (`PAYMENT_PROVIDER` vide) et l'API ne démarrait pas. `loadEnv` retire les valeurs vides avant validation : les défauts s'appliquent, `DATABASE_URL=` reste refusée. Test ajouté |
| 2026-09-24 | Client d'API écrit à la main à l'étape 1, génération typée reportée à l'étape 10 | `packages/api-client` : `createApiClient` sur `fetch` (préfixe `/v1`, `Accept-Language`, jeton porteur, identifiant de corrélation, délai maximal, clé d'idempotence), rejeu unique après rafraîchissement du jeton sur 401, `ApiError` au format `{ code, message, details, correlationId }` avec `status` 0 pour `NETWORK_ERROR`, `TIMEOUT` et `ABORTED` ; 14 tests sur un `fetch` simulé. La description OpenAPI est exportée sans démarrer de serveur (`pnpm openapi`, script `openapi:export` de l'API via `getOpenApiDocument`) dans `src/openapi.json`, commitée. Les types par ressource (openapi-typescript) viendront à l'étape 10, quand les modules existeront |
| 2026-09-24 | Intégration continue et Docker Compose | `.github/workflows/ci.yml` : services PostGIS 16-3.4 et Redis 7, `pnpm install --frozen-lockfile`, migrations et données de départ, lint, typecheck, tests, build, cache pnpm et Turborepo. `infra/docker-compose.yml` (PostGIS, Redis, Mailpit, MinIO) sert à l'intégration continue et aux postes qui ont Docker ; le poste actuel a la ligne de commande Docker sans moteur en marche, les commandes de développement n'en dépendent pas. Sorties de test retirées du cache Turborepo (avertissements « no output files ») |
| 2026-09-24 | Variables ajoutées pour les fournisseurs de repli | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` (textos et numéro vocal, compte Twilio existant recommandé au fondateur plutôt que Telnyx), `BREVO_API_KEY` (courriels, compte existant), `VAPI_PHONE_NUMBER_ID`, `BETTERSTACK_TOKEN`. `.env.example` réécrit avec toutes les variables du schéma, groupées et commentées. Proposé au fondateur le 24 septembre et accepté le soir même (D49) : stockage des documents sur Supabase Storage (protocole S3, région Canada, variables `S3_*`) plutôt que Cloudflare R2, qui n'a aucune région canadienne (D48 : les documents restent au Canada) |
| 2026-09-24 | D49 et D50, décisions du fondateur : Supabase Storage, nouveau compte Twilio | **D49** : les documents des chauffeurs (pièces d'identité, permis, assurances ; plus tard enregistrements audio et vidéo) vont dans Supabase Storage, bucket privé `documents` du projet Supabase, accès par le protocole S3 (`S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`) ; Cloudflare R2 abandonné, les variables `R2_*` restent tolérées par le schéma mais ne sont plus documentées comme cible. Adaptateur `StorageProvider` réel à écrire sur un client S3 compatible (étape 3). **D50** : textos et numéro vocal par Twilio, sur un **nouveau compte Twilio au nom de Neomoov** (`neomoov1@gmail.com`), distinct du compte de Taxi Sylvain ; Telnyx abandonné (`TELNYX_*` tolérées, non documentées). Le même numéro Twilio sera importé dans Vapi pour l'agent vocal. Adaptateur `SmsProvider` réel à écrire sur l'API Twilio (étape 13) |
| 2026-09-24 | Déploiement LWS : images construites sur le serveur, déclenchées par `git push lws main` | VPS KVM M livré (Debian ou Ubuntu avec Docker). Un dépôt Git nu `/opt/neomoov.git` sur le serveur reçoit les poussées du poste ; son crochet `post-receive` extrait dans `/opt/neomoov` et lance `infra/deploy.sh build` (construction des trois images, migrations une seule fois hors des instances, `up -d`, attente des deux instances saines, retour automatique à la version précédente). Aucun identifiant GitHub n'est nécessaire sur le serveur. La publication sur GHCR par GitHub Actions (`.github/workflows/images.yml`) existe en parallèle pour le mode `deploy.sh pull <sha>`, qui demande un jeton de lecture des paquets sur le serveur. Compose : Caddy (TLS Let's Encrypt, `api`, `hub`, `reserver`), API en deux réplicas (le nom `api` est résolu par le DNS de Docker), worker, web, Redis avec persistance ; limites mémoire ; `.env` de production hors Git avec secrets générés par `infra/server-setup.sh` |
| 2026-09-24 | Images Docker sans `pnpm deploy` | Trois étapes par image : installation filtrée complète et construction, installation filtrée `--prod`, image finale `node:24-alpine` qui reçoit les `node_modules` de production et les `dist` construits (plus `packages/db/drizzle` pour les migrations). `pnpm deploy` est évité : son comportement change entre les versions majeures de pnpm. Web en mode `standalone` de Next.js avec `outputFileTracingRoot` à la racine du monorepo ; `NEXT_PUBLIC_API_BASE_URL` est un argument de construction. `dotenv` devient une dépendance de production de `packages/db` (les scripts de migration s'exécutent dans l'image de l'API). `.gitattributes` force LF sur les scripts, Dockerfile et Caddyfile |
| 2026-09-25 | Étape 3 : politique d'accès par défaut « refus », déclarée route par route | Chaque route porte `@Public()`, `@Authenticated()`, `@Roles(...)` ou `@Scopes(...)` ; `assertRoutePolicies` (DiscoveryService et métadonnées NestJS) refuse le démarrage de l'API si une route n'en a aucune, et le test `authorization.e2e` parcourt chaque endpoint (401 sans jeton, 403 pour un rôle non admis, 403 pour un compte de service hors portée). Trois gardes globales dans l'ordre : limite par adresse IP, authentification et politique (rôles, portées, limite par utilisateur), propriété de la ressource (`@Owns('device' \| 'dataRequest' \| 'driverProfile' \| 'ride' \| 'user')`, le personnel voit tout, 404 si la ressource n'existe pas). Un `admin` est admis partout où un rôle du personnel l'est ; sinon les rôles sont explicites |
| 2026-09-25 | Rôles `finance` et `readonly` ajoutés à `user_role` (migration 0002) | Le prompt 03 les nomme pour le second facteur ; la section 4.1 ne les listait pas. `STAFF_ROLES` = admin, operator, finance, readonly. La migration inverse recrée le type sans ces valeurs (PostgreSQL ne retire pas une valeur d'énumération) |
| 2026-09-25 | Jetons : JWT HS256 de 15 minutes (`jose`), rafraîchissement opaque haché, rotation, familles, révocation immédiate | Jeton d'accès signé avec `JWT_ACCESS_SECRET` (`sub`, `sid`, `roles`, `amr`, `typ: access`) ; jeton de rafraîchissement `rt_…` aléatoire (32 octets), stocké en SHA-256 dans `sessions` avec une `family` : la rotation révoque la ligne et en crée une nouvelle dans la famille ; un jeton déjà tourné qui est représenté révoque toute la famille (`REFRESH_TOKEN_REUSED`), une rotation concurrente aussi. Révocation immédiate des jetons d'accès par un drapeau `revoked:<sid>` (Redis, ou mémoire sans Redis) vérifié à chaque requête, d'une durée égale à celle du jeton. Les jetons de passage (second facteur, liaison Apple ou Google) sont des JWT courts signés avec `JWT_REFRESH_SECRET`, à usage unique (`jti` consommé). Durées lues dans `settings` (`auth.*`) |
| 2026-09-25 | Codes SMS : 6 chiffres hachés en HMAC-SHA256 avec `ENCRYPTION_KEY`, un seul code actif par numéro | `otp_codes` ne contient jamais le code en clair ; un nouveau code consomme les précédents ; 5 minutes, 5 tentatives (puis code consommé), 30 s entre deux envois par numéro, 5 par numéro et 20 par adresse IP par heure (réglages `auth.otp_*`). Le code n'est écrit dans le journal qu'en `NODE_ENV=development` (champ `devOtpCode`) ; en test, le fournisseur simulé le conserve. Le texto est produit par `common/i18n.ts` (fr, en) |
| 2026-09-25 | Apple et Google : liaison à un téléphone vérifié, vérification des jetons par les JWKS des fournisseurs | `POST /auth/apple` et `/auth/google` vérifient le jeton d'identité (signature, émetteur, audience `APPLE_CLIENT_IDS` ou `GOOGLE_CLIENT_IDS`, nonce). Compte lié : session. Sinon `{ status: 'phone_required', linkToken }` : l'application demande un code SMS et renvoie `linkToken` à `otp/verify`, qui crée le compte (ou lie l'identifiant au compte du téléphone). Un identifiant déjà lié à un autre compte est refusé (`SOCIAL_ID_ALREADY_LINKED`). `SOCIAL_LOGIN_PROVIDER=mock` accepte `mock-apple:<sujet>:<courriel>` en développement et en test |
| 2026-09-25 | Personnel de My Hub : argon2id en WebAssembly, TOTP RFC 6238 maison, secret chiffré, verrouillage progressif, `amr` en session | Mots de passe argon2id (64 Mio, 3 itérations) par `hash-wasm` : pas de module natif à compiler dans les images Docker. TOTP (SHA-1, 30 s, 6 chiffres, tolérance d'un pas, rejeu interdit par drapeau) écrit dans `common/crypto.ts` sans dépendance ; QR en SVG par `qrcode`. Secret TOTP chiffré AES-256-GCM avec `ENCRYPTION_KEY` (`totp_secret_encrypted`), dix codes de secours hachés. Inscription obligatoire à la première connexion (`mfa_enrollment_required`), vérification ensuite (`mfa_required`). Cinq échecs : 15 minutes de verrou, doublées à chaque série (`auth.staff_lockout_*`) ; réponse identique pour un courriel inconnu (hachage factice vérifié). Colonne `sessions.amr` : les rôles du personnel ne sont accordés qu'aux sessions ouvertes avec `mfa`, y compris après rotation ; une connexion par code SMS d'un membre du personnel ne donne que ses rôles ordinaires, et `GET /me` montre les rôles de la session. Création du premier administrateur par `pnpm --filter @neomoov/api create-staff` (mot de passe par `STAFF_PASSWORD`), manuel `docs/runbooks/personnel-my-hub.md` |
| 2026-09-25 | Comptes de service : clés `nmk_<préfixe hex>_<secret>` à portée limitée (table `api_keys`) | Secret affiché une fois, stocké en SHA-256, recherche par préfixe, comparaison à temps constant, révocation et expiration, `last_used_at` au plus une fois par minute. Portées `agents:run`, `agents:read`, `tools:*` (joker), `rides:read`, … ; une route sans `@Scopes` est fermée aux clés, `@Scopes('*')` accepte toute clé valide. L'acteur d'audit d'une clé est son `agent_code` (ou `key:<nom>`) |
| 2026-09-25 | Journal d'audit : intercepteur global sur toute mutation, colonne `correlation_id`, masquage | Les services enregistrent leurs entrées (`AuditService.record`, avant et après) dans un contexte AsyncLocalStorage ouvert par l'intercepteur ; à la fin de la requête, elles sont écrites avec l'acteur, l'adresse IP et l'identifiant de corrélation. Une mutation sans entrée explicite reçoit une entrée générique `<méthode> <route>` ou celle du décorateur `@Audit`. Masquage récursif des clés sensibles (mot de passe, jeton, secret, code, haché, clé) et troncature des chaînes longues. L'échec de l'écriture est journalisé mais ne fait pas échouer la requête. Lecture par le personnel : `GET /admin/audit` (curseur temporel) |
| 2026-09-25 | Limitation de débit : compteurs à fenêtre fixe, Redis ou mémoire, seuils en base | `RateLimitService` (INCR et EXPIRE sur Redis, `Map` en mémoire sans Redis) : 300 requêtes par adresse IP et par minute avant l'authentification, 600 par utilisateur après (`ratelimit.*`), limites propres aux codes SMS et à la connexion du personnel (10 par courriel et 30 par adresse par 10 minutes), en-têtes `X-RateLimit-*` et `Retry-After`, code `RATE_LIMITED`. Sans Redis, chaque instance compte pour elle-même : en production Redis est obligatoire (déjà exigé) |
| 2026-09-25 | Loi 25 (5.15) dans l'API : consentements versionnés, demandes de droits, export JSON et PDF, suppression par anonymisation | Consentements : une ligne par accord, retrait horodaté, nouvelle version = nouvelle ligne (l'ancienne est close) ; `GET /me/consents` donne l'état par finalité. Politique de confidentialité : version en vigueur dans `settings` (`legal.privacy_policy_version`), exigée à la création du compte et acceptée par `PATCH /me` ; `GET /me` expose `privacyPolicyAccepted`. Demandes (`data_requests`, échéance `privacy.data_request_due_days` = 30 jours en date de Montréal) : accès et portabilité produisent, par la file `privacy`, un export JSON et un PDF (`pdfkit`) déposés dans le `StorageProvider` (`exports/<utilisateur>/<demande>.json|.pdf`), lien signé de 7 jours renvoyé par `GET /me/data-requests/{id}` et envoyé par courriel (sinon texto) ; retrait de consentement traité immédiatement ; la suppression passe par `DELETE /me`. Suppression : statut `deleted` et sessions révoquées tout de suite, puis tâche `account-deletion` : sessions, appareils, codes, identifiants du personnel, adresses et favoris effacés ; téléphone remplacé par `+999` et 11 chiffres dérivés de l'identifiant (le numéro redevient libre), courriel, noms et identifiants Apple ou Google effacés ; courses conservées avec leurs champs de passager effacés ; chauffeur passé à `offboarded`. Le compte du personnel ne se supprime pas depuis l'application. En mode mémoire (sans Redis), `PrivacyJobsService` traite la file dans le processus courant ; avec Redis, dans le worker |
| 2026-09-25 | OpenAPI produite à partir des schémas Zod | `z.toJSONSchema` (Zod 4, cible OpenAPI 3.0) alimente `@nestjs/swagger` par les décorateurs `ZodBody`, `ZodResponse`, `ZodQuery` et `ApiErrors` (réponses d'erreur au format unique) ; les schémas de l'identité vivent dans `packages/domain/src/schemas/auth.ts` (primitives déplacées dans `common.ts`). Le test d'autorisation vérifie que chaque route inventoriée figure dans le document |
| 2026-09-25 | Tests d'intégration sur la base de développement, comptes suivis par fichier | `TEST_DATABASE_URL` reste vide : les tests tournent sur `DATABASE_URL` (Supabase de développement), avec des téléphones `+1999…` et des courriels `@test.neomoov.local`. Chaque fichier de test tourne dans son processus et ne supprime à la fin que les comptes qu'il a créés (identifiants suivis), jamais par motif : la première version supprimait les comptes des autres fichiers en cours d'exécution. Le délai entre deux codes SMS est neutralisé dans l'aide de test en antidatant les codes précédents. Les lignes d'audit des tests restent (table en ajout seul) |
| 2026-09-25 | Revue de code de l'étape 3 : 15 constats corrigés | Le plus grave : `SOCIAL_LOGIN_PROVIDER` valait `mock` par défaut sans refus en production, ce qui aurait accepté des jetons forgés (`mock-apple:<sujet>`) et permis de prendre tout compte lié ; `loadEnv` exige maintenant `real` en production et `infra/server-setup.sh` l'écrit. Second facteur : le verrouillage est vérifié à chaque étape (TOTP, code de secours, confirmation) et le compteur d'échecs n'est remis à zéro qu'après le second facteur. Suppression de compte : si le worker n'a pas encore anonymisé le numéro, une reconnexion avec ce numéro le libère (`releasePhone`) avant de créer le nouveau compte ; toute violation d'unicité à la création devient 409 `ACCOUNT_CONFLICT`. Courriel remis par Apple ou Google : repris seulement s'il est vérifié par le fournisseur et libre. Audit : les entrées sont écrites avant l'envoi de la réponse, et celles enregistrées avant une exception sont conservées ; curseur de pagination sur (instant, identifiant) pour ne sauter aucune entrée partageant le même instant. Rafraîchissement : le compte est vérifié avant la rotation (plus de session orpheline pour un compte bloqué). Journal : `*.code` et `*.key` retirés du masquage (`err.code` doit rester lisible). `create-staff` met à jour un compte existant par courriel ou téléphone et affiche une erreur propre. Jeton de liaison Apple ou Google vérifié avant de consommer le code SMS, puis à usage unique. Code mort `OTP_LOCKED` remplacé : le cinquième échec renvoie 429 `OTP_LOCKED` et consomme le code. Seuils de la connexion du personnel et du jeton de liaison déplacés dans `settings` (`auth.staff_login_per_*`, `auth.link_token_ttl_seconds`) ; la durée de rejeu TOTP (90 s) reste une constante de protocole. Export PDF traduit (fr, en) et prénom échappé dans les courriels HTML ; `outcome` d'une demande est une note d'exploitation, l'utilisateur reçoit `status`. Appareils sans jeton push dédoublonnés par utilisateur et plateforme. Tests : les codes SMS de test sont retirés à la fin de chaque fichier |
| 2026-09-25 | Étape 4 : péages (D33) dans le moteur pur, ignorés sous un forfait | `QuoteInput.tollsCents` (montant réel de l'API Routes) devient une ligne `tolls` du prix affiché, ajoutée au sous-total avant taxes et jamais au tarif chauffeur. Un forfait est « tout compris » : les péages y sont ignorés (`tollsCents: 0`), comme les options. Un péage négatif est une entrée invalide |
| 2026-09-25 | Vérification concurrentielle (D33) : `benchmarkCheck` pure, plages horaires, référence la plus récente | `packages/domain/src/pricing/benchmark.ts` : six plages horaires de Montréal (`weekday_morning` 6 h à 10 h, `weekday_day` 10 h à 15 h, `weekday_evening` 15 h à 19 h, `weekday_night`, `weekend_day` 6 h à 19 h, `weekend_night`), référence = concurrent le moins cher du relevé le plus récent de moins de `pricing.benchmark_max_age_days` (14) pour la même catégorie, les mêmes zones (dans les deux sens) et la même plage. Si le total dépasse `référence × (1 − pricing.benchmark_margin_ppm)`, ligne `benchmark_alignment` négative qui réduit les frais de service jusqu'à 0 (jamais le tarif chauffeur, jamais la redevance), taxes et total recalculés, crédits plafonnés au nouveau total. `exceeded` reste vrai même sans marge de remise : événement `pricing.benchmark_exceeded` dans le journal d'audit et le journal applicatif (alerte quotidienne à l'étape 13). Table `competitor_benchmarks` (migration 0003) saisie par le personnel : `POST /admin/pricing/benchmarks`, liste, suppression ; aucune API d'Uber ou de Lyft n'est appelée |
| 2026-09-25 | Promotions : plafond de remise dans le moteur, traduction des lignes `promotions` par le service de devis | `Promotion.maxDiscountCents` plafonne une remise en pourcentage (LANCEMENT30 : 15,00 $). Le service traduit une ligne de la table : `nth_ride` → course offerte au n-ième trajet (`waivesFees` selon la ligne), `percent` → remise en points de base plafonnée, `free_ride` → course offerte ; `fixed` n'est pas admis sur un devis. Conditions vérifiées en base avant le moteur : promotion active et valide (`PROMO_CODE_UNKNOWN`), budget, limite globale et limite par client (`PROMOTION_NOT_APPLICABLE`) ; distance et rang de course par le moteur. Le nombre de courses terminées vient de `clients.ride_count`, les crédits de `credits.remaining_cents` non expirés |
| 2026-09-25 | Devis : préavis D32 appliqué au devis, toutes les catégories par défaut, persistance avec empreinte, mode dégradé | `POST /v1/quotes` sans `category` tarife toutes les catégories actives de la grille (trois en V1), même itinéraire, un enregistrement `quotes` par catégorie (empreinte SHA-256 des entrées, des lignes et de la version des règles ; validité `pricing.quote_validity_seconds`). `requestedAt` doit respecter `rides.min_lead_seconds` (7200) et `rides.max_lead_days` (30) : `LEAD_TIME_TOO_SHORT` avec `earliestPickupAt`, `LEAD_TIME_TOO_LONG` ; sans heure, course immédiate seulement si `FEATURE_IMMEDIATE_RIDES` est actif. Hors aire de service (aucune zone pour l'origine ni la destination) : `OUT_OF_SERVICE_AREA`. Itinéraire par `MapsProvider.route` avec `departureTime` = heure de prise en charge ; en cas d'échec (nouvelles tentatives et disjoncteur épuisés), estimation interne (vol d'oiseau × `pricing.degraded_distance_factor_bps`, vitesse `pricing.degraded_speed_kmh`), `estimated: true` sur la réponse et chaque devis, persisté. Les lignes renvoyées portent un `code` stable et un libellé de repli (fr, en) ; la somme des lignes vaut `amountDueCents`. `GET /v1/quotes/{id}` appartient au client du devis (`@Owns('quote')`) |
| 2026-09-25 | Zones en mémoire, temps d'arrivée provisoire, règles versionnées | `ZonesService` charge les polygones actifs (GeoJSON) et résout un point en JavaScript (lancer de rayon), cache de 5 minutes, priorité aéroport > centre-ville > quartier > aire de service ; PostGIS reste la source (`zones_containing` sert aux requêtes SQL). `PricingRulesService` assemble `buildPricingRules` depuis `settings`, `pricing_rules`, `surcharges` et `flat_rates` (jointure des codes de zones), cache 60 s, version = empreinte des sources (16 caractères) enregistrée sur chaque devis ; `POST /admin/pricing/rules/refresh` vide les caches. Temps d'arrivée : jusqu'à cinq chauffeurs `driver_presence` disponibles de la catégorie, actifs, dans `pricing.eta_search_radius_m`, puis `etaMatrix` ; sans chauffeur, `eta.status = on_availability` (« selon disponibilité ») |
| 2026-09-25 | Adaptateur Google Maps Platform réel (`GoogleMapsProvider`) | Routes API `computeRoutes` (`TRAFFIC_AWARE`, `departureTime` si dans le futur, `extraComputations: TOLLS`, péages en CAD convertis en cents, polyligne), `computeRouteMatrix` pour les temps d'arrivée (origine injoignable = une journée), Places nouvelle version (`places:autocomplete` avec jeton de session et biais de position, `places/{id}` avec `FieldMask`), Geocoding. Délai de 3 s par appel (`AbortSignal.timeout`), deux nouvelles tentatives sur erreur réseau, 408, 429 et 5xx (attente 200 ms puis 400 ms), aucune sur 4xx définitif (`MAPS_REQUEST_REJECTED`, 502), disjoncteur ouvert 30 s après cinq échecs consécutifs (`MAPS_UNAVAILABLE`, 503, sans appel réseau). Testé avec un `fetch` simulé ; la clé serveur n'est jamais exposée aux applications. `MapsProvider` gagne `placeDetails` et un point de proximité pour l'autocomplétion ; le simulateur peut échouer sur commande (`failRoutes`) pour tester le mode dégradé |
| 2026-09-25 | Amendements v1.1 au schéma appliqués (migration 0003) | Tables des amendements du prompt 02 qui manquaient : `competitor_benchmarks`, `organizations` (D42, ligne `neomoov` semée) avec `organization_id` nullable sur `drivers`, `vehicles`, `rides`, `weekly_statements` et `settings`, `client_driver_links` (D40) ; colonnes `payment_choice` (D36, `prepaid` ou `pay_driver_after`), `installment_provider_ref` et `tolls_cents` sur `rides` ; réglages du 23 septembre semés (`rides.min_lead_seconds` 7200, `pricing.negotiation_*`, `pricing.benchmark_margin_ppm`, `payments.installment_min_cents`) et drapeaux `immediate_rides`, `negotiation_above_max`, `installments`, `ride_series` désactivés. Reportés à leurs étapes : `ride_series` (prompt 05), `installment_plans` (07), `interview_results` et `recruitment_criteria` (13), `recording_consents` et `recordings` (14, V2) |
| 2026-09-25 | Schémas Zod des lieux et des devis déplacés dans `schemas/quotes.ts` | `coordinatesSchema`, `placeSchema`, `rideOptionsSchema`, `ridePreferencesSchema`, `quoteRequestSchema` (catégorie facultative), `quoteSchema` (péages, promotion, remise d'alignement, `amountDueCents`, `estimated`, `eta`), `quotesResponseSchema`, `quoteDetailSchema`, `simulateQuoteSchema`, schémas des lieux et des relevés. `index.ts` les réexporte ; aucun écran ne les consommait encore |
| 2026-09-25 | Revue de code de l'étape 4 : 15 constats corrigés | Détail du devis : la ligne « frais de service » affiche les frais avant remise d'alignement, la ligne négative `benchmark_alignment` porte la remise, et la somme des lignes vaut de nouveau le reste à payer (vérifié par le test D33). `POST /v1/quotes` ne renvoie plus les références concurrentielles ni la version des règles (réservées à la simulation My Hub). Une promotion réservée à certaines catégories n'est simplement pas appliquée aux autres quand toutes les catégories sont tarifées. Les relevés sont filtrés par catégories et plage horaire avant la limite de lecture. Une origine injoignable dans la matrice de temps vaut l'infini et n'est jamais affichée comme un temps d'arrivée. La ligne de forfait a le code stable `flat_rate` et `flatRateCode` vaut le code de la table `flat_rates` (`codeByCategory` dans les règles). Grilles, suppléments et forfaits filtrés sur `valid_from` (la ligne la plus récente déjà en vigueur gagne). Colonne `quotes.created_by_user_id` (migration 0003) : le demandeur sans profil client relit son devis. Aucun événement `pricing.benchmark_exceeded` pendant une simulation. Message du préavis calculé à partir du réglage. `ignoreLeadTime` couvre aussi le préavis maximal ; distance et durée forcées ensemble ou pas du tout. Temps d'arrivée calculés en parallèle et devis insérés en une seule instruction. `haversineMeters` et `pointInPolygon` dans `common/geo.ts`, partagées par le simulateur de cartes et le service. Le test du temps d'arrivée restaure le statut du chauffeur de démonstration. Le simulateur de cartes renvoie 404 `PLACE_NOT_FOUND` pour un lieu inconnu et n'appelle `geocode` qu'en l'absence de correspondance connue |
| 2026-09-25 | Étape 5 : l'état d'une course ne change que par la machine à états, sous verrou, avec idempotence | `RidesService.applyTransition` verrouille la ligne (`FOR UPDATE`), applique `transition()` du domaine, met à jour `state` et `state_timestamps` (horodatage de chaque état atteint) et écrit `ride_events` (type = événement, états avant et après, acteur `client`, `driver`, `operator`, `system` ou `agent`, données et effets déclarés). Rejouer la transition qui a produit l'état courant (dernier événement identique) renvoie la course sans nouvel événement ; toute autre transition impossible est un 409 `RIDE_INVALID_TRANSITION`. Aucune écriture directe de `state` ailleurs. L'attribution forcée depuis `requested` passe par `offers_sent` (système) puis `driver_accepts` (opérateur ou chauffeur), après vérification de la garantie modèle (`vehicle_categories.rank`) |
| 2026-09-25 | Création d'une course : devis du demandeur, clé d'idempotence en base, numéro par la fonction SQL | `POST /v1/rides` exige l'en-tête `Idempotency-Key` (8 à 80 caractères) : colonne `rides.idempotency_key` unique, la même clé renvoie la même course (200) ; le devis doit appartenir au demandeur, être valide, non déjà utilisé et porter le même prix maximal consenti ; le type suit `quotes.requested_at` (planifiée) et une course immédiate exige `FEATURE_IMMEDIATE_RIDES`. Numéro public par `next_ride_public_number(fuseau)`, montants copiés du devis, `created_by_user_id`, `organization_id` Neomoov. Création par l'opérateur (`POST /v1/admin/rides`) avec fiche minimale (`guest_name`, `guest_phone`, `guest_language`) ou compte client |
| 2026-09-25 | Frais d'annulation et non-présentation : fonctions pures, montants et délais en base | `packages/domain/src/rides/cancellation.ts` : `clientCancellationFeeCents` (0 avant l'attribution ou dans `rides.free_cancellation_seconds` après elle, sinon `rides.cancellation_fee_cents`), `noShowCheck` (au moins `rides.no_show_min_wait_seconds` sur place et `rides.no_show_min_contacts` tentatives : `POST /driver/rides/{id}/contact` et messages du chauffeur sur place comptent, colonne `contact_attempts`), `waitedSecondsBetween`. L'annulation par le chauffeur (`driver_cancels`) est suivie immédiatement de `reassign` (système) : la course redevient `requested` sans chauffeur, l'événement `ride.reassign_requested` est publié pour la répartition (étape 6), l'effet `driver_sanction` est journalisé en audit |
| 2026-09-25 | Prix final et trace de course | À `complete`, `finalizeQuote` du domaine ajoute l'attente (`waited_seconds`, mesurée entre `arrived` et `in_progress`) dans la limite du prix maximal consenti ; `final_price_cents`, `wait_charge_cents`, tarif et taxes mis à jour ; compteurs `drivers.ride_count`, `clients.ride_count` et lien `client_driver_links` (D40) alimentés ; événement `ride.completed` publié (paiement et facture aux étapes 7 et 9). Trace : positions `driver_locations` de la course (`ride_id` posé pendant les états actifs), simplifiées par Douglas-Peucker (10 m) dans `ride_tracks` avec distance et durée mesurées ; sans trace (moins de deux points), les mesures de l'application ou du devis servent |
| 2026-09-25 | Présence et positions : PostGIS source de vérité, Redis GEO en appoint, historique par lots | `driver_presence` (une ligne par chauffeur en ligne, disponible ou en pause) est mise à jour à chaque position ; `presence:geo` (Redis GEOADD) suit quand Redis existe. Passage en ligne : chauffeur actif, véhicule actif du chauffeur, documents `drivers.required_documents` approuvés et non expirés, pack actif si `drivers.require_active_pack`, aucune suspension ni solde bloquant ; refus 409 `DRIVER_NOT_ELIGIBLE` avec la liste des motifs ; position requise. Positions : rejetées (non comptées) si moins de `presence.min_distance_meters` et moins de 5 s après la précédente ; écrites par lots toutes les `presence.location_batch_ms` dans `driver_locations` (partition du jour créée par `ensure_driver_locations_partition`) ; présence expirée après `presence.expiry_seconds` sans position (balayage toutes les 30 s), quart `driver_shifts` fermé. Diffusion par l'événement `driver.location` : client de la course en cours seulement, et My Hub |
| 2026-09-25 | Temps réel : trois espaces Socket.IO, jeton à la connexion, salles, adaptateur Redis | `/client` (`ride.subscribe` après vérification que l'utilisateur participe à la course, `ride.updated`, `driver.location`, `message.received`), `/driver` (salle `driver:<id>` automatique, `status.update`, `location.update`, `ride.subscribe`), `/admin` (personnel seulement, salle `admin` : `ride.updated`, `driver.location`, `driver.presence`, `alert.new`). Jeton d'accès obligatoire dans `auth.token` (ou en-tête), sinon `error` puis déconnexion ; messages validés par Zod et acquittés `{ ok, … }`. Les événements de domaine (`DomainEventsService`, en mémoire) sont relayés par `RealtimeService` ; avec `REDIS_URL`, `RedisIoAdapter` partage salles et émissions entre les instances de l'API. Test : position chauffeur reçue par le client en moins de 2 secondes ; charge légère de 8 chauffeurs à 4 positions par seconde (200 chauffeurs pendant 2 minutes avec `LOAD_TEST=1`), aucune perte |
| 2026-09-25 | Réservation planifiée : signaux du worker, une fois chacun, et proposition volontaire des chauffeurs | `ScheduledService.tick(now)` (file `scheduling`, toutes les minutes dans le worker ; `POST /admin/rides/scheduled/tick` pour une passe manuelle) : rappel J-1 (`rides.scheduled_reminder_before_seconds`, pour les courses réservées plus de 24 h à l'avance), `scheduled.dispatch_due` à `rides.scheduled_assign_before_seconds` sans chauffeur (consommé à l'étape 6), alerte opérateur à `rides.scheduled_reassign_before_seconds` si rien n'est confirmé (`alert.new` My Hub, notification au personnel, `scheduled_assignments.operator_alerted_at`) ; chaque signal est un `ride_events` de type `scheduled_*`, jamais répété. `GET /driver/scheduled` liste les courses ouvertes de la catégorie du chauffeur (ou d'un rang inférieur) et les siennes ; `claim` crée une proposition (une seule active par course), `confirm` attribue par la machine à états, `decline` la retire. Les offres automatiques sur le créneau (D40) arrivent avec la répartition |
| 2026-09-25 | Messagerie masquée, partage, SOS, notifications en file | Messages dans `ride_messages` (client, chauffeur, opérateur ; fermés dans un état terminal), relayés en temps réel ; aucun numéro échangé (proxy vocal à l'étape 13). Partage : `tracking_token` (20 caractères) et `GET /v1/public/track/{token}` sans authentification : numéro de course, état, catégorie, destination, prénom et véhicule du chauffeur, sa position pendant les états actifs, expiré `rides.share_link_ttl_hours` après la fin. SOS : incident `type sos`, gravité `critical`, événement `ride.sos`, alerte `alert.new` My Hub, texto au personnel (file). Toutes les notifications de la section 5.14 sont mises en file dans `notifications` (gabarit, données, destinataire ou adresse pour un invité) ; l'envoi réel est branché à l'étape 13 |
| 2026-09-25 | Tests d'intégration des courses : nettoyage avec le déclencheur d'ajout seul suspendu | `ride_events` refuse toute suppression ; le nettoyage des comptes de test suspend le déclencheur dans une transaction (verrou exclusif bref) pour retirer les courses de test, puis le rétablit. Délai des tests d'API porté à 120 s (base distante, création de chauffeurs et de personnel avec argon2id) |
| 2026-09-25 | Revue de code de l'étape 5 : 15 constats corrigés | Détection du rejeu dans `applyTransition` limitée aux événements de transition (un message ou une position ne masque plus une transition). `assign` refuse (409 `RIDE_ALREADY_ASSIGNED`) une course active déjà attribuée à un autre chauffeur ; à la réattribution, l'ancien chauffeur quitte la salle de la course (`previousDriverId`). Frais d'annulation client calculés sous le verrou de la ligne. Une course d'invité n'a pas de compte client : notifications au téléphone de l'invité. Vue de course : véhicule pris par `rides.vehicle_id`, pas le véhicule courant du chauffeur. Authentification des sockets dans l'intergiciel d'espace (`server.use`) : refus en `connect_error` avec le code (`UNAUTHENTICATED`, `DRIVER_PROFILE_REQUIRED`, `FORBIDDEN_ROLE`) ; gardes HTTP et journal d'audit ignorés hors HTTP. Bus d'événements : origine par instance pour ignorer ses propres messages Redis, dates ISO restituées. Un seul adaptateur Socket.IO (`AppIoAdapter` : CORS de l'API, adaptateur Redis, fermeture des connexions à l'arrêt). Positions : intervalle minimal `presence.min_interval_seconds` et distance `presence.min_distance_meters` en base ; lot accumulé hors ligne par `POST /driver/locations` ; pendant une course active, une présence expirée ou retirée par un passage hors ligne est recréée en pause (le suivi du client continue, aucune nouvelle offre) ; `DRIVER_OFFLINE` sans course active ; `/driver/location(s)` hors du journal d'audit (`@NoAudit`). Planifiées : une seule requête `ride_events` par passe, proposition en double → 409. Index partiel `rides_quote_unique` (un devis, une course), migration 0004 regénérée avec son script de retour. Tests : refus de connexion lus dans `connect_error`, attribution attendue jusqu'à `assigned`, heure des simulations fixée à 14 h UTC (majoration de nuit de 23 h à 5 h, heure de Montréal), chaîne du seed corrigée. |
| 2026-09-25 | Étape 6 : répartition automatique portée par un seul processus, état en base, horloge simulable | `DispatchService` : sans Redis, l'API porte la répartition (abonnements au bus et battement toutes les `DISPATCH_TICK_MS`) ; avec Redis, c'est le worker (`DispatchWorker` appelle `enableRunner()`) et l'API s'en retire. `DISPATCH_MODE=manual` : aucune répartition à la demande (opérateur et tests). Tout l'état est en base (`ride_dispatches`, `ride_offers`, `ride_events`, migration 0005) ; `tick(now)` fait avancer offres échues, pas dus, fenêtres closes et surveillance du départ, avec la même heure pour tout ce qu'il déclenche (`POST /admin/rides/dispatch/tick` pour une passe manuelle). Verrou par course : Redis (`SET NX PX`) ou chaîne de promesses en mémoire ; l'attribution reste garantie par le verrou de ligne de `RidesService.assign` : la première acceptation gagne, les suivantes reçoivent 409 `OFFER_EXPIRED` sans rien écrire (ni prix convenu, ni journal) |
| 2026-09-25 | Course immédiate : offres séquentielles par rayons, balayages avant « aucun chauffeur » | Rayons `dispatch.search_radii_m` (2, 5, 10 km puis la zone). Candidats en une requête : présence disponible (ou fin de course à moins de `dispatch.chain_max_seconds` de l'origine), catégorie égale ou supérieure, documents exigés, pack si exigé, solde, suspension, aucune offre en attente ailleurs, aucune course déjà engagée, mode de paiement accepté (amendement v1.1). Temps d'arrivée par matrice pour les `dispatch.eta_candidates` plus proches ; les suivants sont estimés par la distance sans descendre sous le plus long temps mesuré (sinon l'estimation brute, sans trafic, les faisait passer devant les plus proches). Score du domaine (5.4), `dispatch.candidates_per_wave` par vague, une offre de `dispatch.offer_seconds` à la fois ; un refus sollicite le suivant tout de suite. Rayon vide : le suivant tout de suite. Zone épuisée sans aucune offre envoyée : `dispatch.empty_sweeps_max` (3) balayages complets espacés de `dispatch.wave_seconds`, puis `no_driver` ; avec des offres refusées ou expirées, `no_driver` dès la zone épuisée. `no_driver` prévient le client (`ride.no_driver`) et l'exploitation (`alert.no_driver`). Réattribution (annulation du chauffeur, absence de mouvement, opérateur) : chauffeur exclu, priorité (en tête du battement, recherche d'emblée au deuxième rayon, qui contient le premier). `zone_imbalance` vaut 0 en V1 |
| 2026-09-25 | Réservation planifiée (D40) : diffusion dès la réservation, favori exclusif | À la réservation, fenêtre de `dispatch.scheduled_window_seconds` (10 min) : offres simultanées aux chauffeurs qui acceptent les planifiées (catégorie, mode de paiement, aucune planifiée attribuée à ± `dispatch.scheduled_conflict_minutes`), le favori (demandé dans le devis ou favori du client) seul pendant `dispatch.favourite_exclusive_seconds` (120 s). Sans confirmation, la fenêtre se ferme (`offer_window_closed`) et la course reste demandée ; le signal `scheduled.dispatch_due` (60 minutes avant) rouvre une fenêtre. Une planifiée attribuée n'est pas surveillée pour l'absence de mouvement (le rappel et l'alerte à 30 minutes s'en chargent) |
| 2026-09-25 | Négociation encadrée (5.5, D34) sous `FEATURE_NEGOTIATION` | `POST /rides/{id}/proposals` : groupe de test tiré au sort une fois par client (`clients.experiment_group`, moitié en négociation, moitié au prix fixe) ; exclusions : forfait, compte entreprise ou organisation partenaire (la plateforme Neomoov, posée sur chaque course, n'en est pas une), lot, Neo Limo ; P' borné au plancher `pricing.negotiation_floor_ppm` (70 %) et arrondi au dollar. P' et P sont diffusés en même temps aux `negotiation.candidates` meilleurs candidats pendant la fenêtre (`negotiation.window_seconds`, 10 min, pour une planifiée ; `negotiation.immediate_window_seconds`, 60 s, pour une immédiate ; `negotiation_ends_at` = fin de fenêtre). Le chauffeur accepte P', décline, ou contre-propose une seule fois entre P' et P (au-dessus de P seulement avec `FEATURE_NEGOTIATION_ABOVE_MAX`, un motif et sous `pricing.negotiation_ceiling_ppm`). Le client retient une contre-offre (`POST /rides/{id}/offers/{offerId}/accept` ; au-dessus de P, acceptation écrite, prix maximal consenti relevé et audité). Prix convenu (`rides.agreed_total_cents`) porté par le tarif du chauffeur, taxes recalculées ; prix final = prix convenu. Fin de fenêtre sans accord : repli automatique au prix affiché (`negotiation_fallback`, notification au client), offres au prix fixe. Drapeau inactif : 404 `FEATURE_DISABLED`, `negotiation: null` dans la vue, `offers.updated` jamais émis |
| 2026-09-25 | Panneau opérateur, surveillance du départ, garantie modèle | `GET /admin/rides/{id}/dispatch` (état, vague, rayon, toutes les offres), `hold` (offres retirées, aucune relance jusqu'à `release`), `release`, `reassign` (chauffeur retiré sans sanction, exclusion facultative, recherche prioritaire). Surveillance : chauffeur à moins de `dispatch.no_movement_meters` de sa position d'attribution après `dispatch.no_movement_seconds` : retrait sans sanction, notification `ride.removed_no_movement`, réattribution. À l'attribution, toutes les offres encore ouvertes de la course sont closes, y compris la proposition du chauffeur retenu quand le client a choisi sa contre-offre. Signalement d'un véhicule non conforme par le client (`POST /rides/{id}/report-vehicle-mismatch`) : incident `model_guarantee`, un par course, dans les `rides.vehicle_mismatch_window_hours` (24 h) qui suivent la course, alerte `alert.vehicle_mismatch` ; remboursement de l'écart à l'étape 8 |
| 2026-09-25 | Latence de la première offre : allers-retours comptés, pool réglable | Mesuré sur la base de développement (environ 45 ms par aller-retour depuis le poste) : 6,9 s entre la création de la course et la première offre avec 2 connexions, surtout en attente de connexion et en relectures. Corrigé : premier pas avec la course et la répartition déjà lues, signal de journal écrit en une requête (`INSERT … SELECT` dans `RidesService.mark`), type d'organisation en cache, contexte « aéroport ou entreprise » calculé une fois par pas et seulement s'il change le score, offre publiée (bus, socket) avant son journal et sa notification. Il reste une quinzaine d'allers-retours avant l'offre : l'exigence de 3 s suppose une base proche de l'API (à trancher avec l'hébergement, D48). `DATABASE_POOL_MAX` règle le pool (défaut 10, 2 en test, 5 pour le test de la répartition) |
| 2026-09-25 | Test de la répartition isolé des autres fichiers | `dispatch.e2e.test.ts` démarre l'API en `DISPATCH_MODE=auto` sans battement et avance l'horloge par `tick(now)`. Les autres fichiers tournent en même temps sur la même base : toutes ses courses sont payées au chauffeur par terminal et seuls ses chauffeurs acceptent le terminal. À la fin de chaque scénario, répartitions arrêtées, offres retirées, chauffeurs hors ligne et terminal refusé, pour que le battement d'un scénario ne touche pas les courses du précédent. `TEST_LOG_LEVEL=error` affiche les erreurs des traitements asynchrones, muettes sinon |
| 2026-09-25 | Chauffeur favori indisponible (D37) : client prévenu, supplément retiré | Quand la course est attribuée à un autre chauffeur que le favori demandé dans le devis (hors ligne, occupé, refus ou délai dépassé), le signal `favourite_unavailable` est journalisé et le client reçoit `ride.favourite_unavailable`, une seule fois par course ; pas avant l'attribution, puisque le favori peut encore être sollicité à une vague suivante ou dans une diffusion. La course part à un autre favori du client (bonus du score, exclusivité des planifiées), sinon au meilleur candidat. À la fin de course, si le chauffeur n'est pas le favori demandé, le montant exact de la ligne `favourite_driver` du devis est retiré du tarif, taxes recalculées ; un prix négocié fait foi et n'est pas touché |
| 2026-09-25 | Points PostGIS lus bruts : décodage du WKB | `geoPoint` décodait seulement le GeoJSON de `ST_AsGeoJSON` ; une lecture brute (`select()` de `ride_dispatches`, dont `assigned_position`) recevait le WKB hexadécimal et levait une erreur, ce qui faisait échouer en 500 toute attribution suivie par la répartition. `pointFromEwkbHex` (ordre des octets, drapeau SRID) décode maintenant les deux formes ; test unitaire dans `packages/db` |
| 2026-09-25 | Sélection précise du véhicule (D37) en V1 : chauffeurs libres sur le créneau, choix porté par la course | `GET /v1/quotes/{id}/vehicles` (propriétaire du devis) liste les véhicules libres sur le créneau d'un devis planifié : catégorie réservée ou supérieure (garantie modèle), chauffeur actif qui accepte les planifiées, documents et solde en règle, sans autre planifiée à ± `dispatch.scheduled_conflict_minutes` ; favoris du client d'abord, puis la note ; modes « payer le chauffeur après » acceptés par chaque chauffeur ; `photoUrl` vide en attendant les photos de l'application chauffeur (étape 11). Course immédiate : liste vide. Les disponibilités déclarées par les chauffeurs (cahier des charges 5.2) remplaceront ce filtre avec l'application chauffeur. `POST /v1/rides` accepte `vehicleId` pour une planifiée seulement (sinon 400 `VEHICLE_CHOICE_SCHEDULED_ONLY`), vérifié avec les mêmes règles que la liste et la répartition (`eligibility.ts` : véhicule courant actif, chauffeur éligible et libre sur le créneau, mode de paiement accepté), sinon 409 `VEHICLE_NOT_AVAILABLE` ; catégorie inférieure, 409 `VEHICLE_CATEGORY_TOO_LOW`. Le choix est copié dans `rides.options` (`requestedVehicleId`, `requestedDriverId`) ; la répartition traite ce chauffeur comme le favori demandé (bonus, exclusivité des planifiées), puis la catégorie ; le client est prévenu une fois, à l'attribution à un autre chauffeur (`ride.vehicle_unavailable`), sans supplément à retirer |
| 2026-09-25 | Revue de code de l'étape 6 : 10 constats corrigés | Chaque candidat est revérifié juste avant son offre (le raccourci qui l'évitait laissait deux courses voisines solliciter le même chauffeur). Avis « favori » ou « véhicule » indisponible envoyé à l'attribution, plus à la première offre faite à un autre chauffeur. Véhicule choisi validé par les mêmes filtres SQL que la liste et la répartition, réunis dans `eligibility.ts` (éligibilité, mode de paiement, catégorie, créneau libre). Offres restantes du chauffeur retenu closes sans signal `offer.expired`. Crédits plafonnés au total quand la remise du favori le fait baisser, enregistrés à la fin de course. Exclusivité d'une planifiée au chauffeur demandé avant tout autre favori du client. `markAssigned` : un seul passage par attribution (l'événement `ride.assigned` est ignoré quand l'acceptation clôt elle-même la répartition, ou si elle l'a été il y a moins de 30 s), course déjà lue passée en paramètre. Contexte premium calculé une fois par pas (aéroports en parallèle). Aucun prénom de repli codé en dur dans l'API : `firstName` peut être nul, l'application affiche son libellé traduit |
| 2026-09-25 | Étape 10 : configuration publique et profil client côté API | `GET /v1/config` (public) : drapeaux distants lus dans l'environnement (négociation, prix au-dessus du maximum, courses immédiates, échelonné), préavis, annulation, négociation, assistance (`support.phone`, `support.email`), version de la politique et pages légales (`legal.*`, neomoov.net/conditions-d-utilisation et /politique-de-confidentialite), catégories actives dans l'ordre du rang. L'application la relit au démarrage : un drapeau change sans nouvelle version. `GET/PUT /v1/me/preferences` (préférences D36, D37 : ambiance, musique et genre, température, langue du chauffeur, bagages avec nombre et taille, siège enfant, mobilité réduite ; valeur ancienne invalide remplacée par les défauts), `/v1/me/places` (20 lieux au plus, `clients.saved_places_max`) |
| 2026-09-25 | Client d'API : ressources typées par le domaine, file hors ligne | `client.auth`, `config`, `me`, `places`, `quotes`, `rides` : un appel HTTP par méthode, types des schémas Zod de `@neomoov/domain` (les mêmes que ceux qui valident l'API) plutôt qu'un générateur OpenAPI. `OfflineQueue` : une écriture (évaluation, message) faite sans réseau est gardée dans le stockage de l'application et rejouée dans l'ordre avec sa clé d'idempotence ; une panne réseau ou une erreur 5xx arrête le rejeu, un refus 4xx abandonne l'écriture. `fetch` est toujours appelé sur l'objet global (le navigateur refuse un appel comme méthode d'un autre objet, « Illegal invocation », que le client prenait pour une panne réseau). 30 s pour le devis et la création de course (itinéraire Google), 15 s ailleurs ; un délai dépassé est annoncé comme tel |
| 2026-09-25 | Application client : Expo Router, React Query, Zustand, aucune logique de prix | Session dans SecureStore (localStorage sur le web, démonstration seulement) avec rotation des jetons par le client d'API ; brouillon de réservation partagé par les trois écrans (Zustand) ; données serveur par React Query (la répétition n'a lieu que sur panne réseau ou 5xx). Le détail du prix affiche les lignes de l'API telles quelles (somme = montant dû, testée) ; les options, arrêts et changements d'adresse redemandent un devis. Le créneau se choisit par jour, heure puis quart d'heure, à partir de maintenant + préavis. Carte : `react-native-maps` (Apple Plans sur iOS, clé Android lue au build par `app.config.ts`), version web de repli sans carte. Suivi en direct par le socket `/client` ; sans socket, rafraîchissement HTTP toutes les 5 s (7.3), arrêté quand la course est close |
| 2026-09-25 | Connexion : acceptation des conditions demandée à chaque code | L'API consomme le code avant de savoir si le compte doit être créé ; vérifier les conditions avant le code révélerait quels numéros ont un compte. L'écran du code affiche donc toujours la case « J'accepte… » (obligatoire), ignorée par l'API pour un compte existant. Un nouveau compte passe ensuite par les consentements, aucun coché d'avance |
| 2026-09-25 | Réservation pour un tiers et arrêts | Écran 2 : « Je réserve pour quelqu'un d'autre » (nom, téléphone), transmis comme `passenger` ; à l'attribution, l'API envoie au passager par texto le lien de suivi public (`ride.passenger_tracking`, même jeton que « Partager le trajet », créé au premier besoin). Arrêts : trois au plus, tarifés par le devis |
| 2026-09-25 | Notifications push et liens profonds | Jeton Expo déclaré à `POST /me/devices` à chaque ouverture de session ; bannière au premier plan ; canal Android ; une notification qui porte `rideId` ouvre la course. Sans `extra.eas.projectId` (projet EAS à créer par le fondateur), sur le web et sur simulateur, l'enregistrement est ignoré sans erreur |
| 2026-09-25 | Vérification de l'application sans simulateur : parcours web réels et bundles | Le poste n'a ni Maestro ni SDK Android. Les parcours Maestro 1, 3, 4, 5 et 6 sont écrits (`maestro/`, identifiants de test sur les champs et les choix) ; l'équivalent est exécuté sur la version web par `e2e/web-journeys.cjs` (Edge sans interface, gestes réels par le protocole DevTools) contre l'API locale avec Google Maps réel : création du compte, consentements, réservation en trois écrans, tiers, course réservée et répartie, réservations, profil ; captures dans `docs/screens/client/`. Les bundles web et Android (Hermes) sont construits à chaque vérification. Les composants critiques sont testés par leur logique pure (détail du prix, sélecteur de catégorie, créneaux, négociation, formats) |
| 2026-09-25 | Démarrage de l'API : zones et règles préchargées | `PricingWarmupService` charge les zones et les règles de tarification au démarrage, sans le bloquer (jamais en test) : le premier devis après un redémarrage dépassait 15 s avec la base distante. En développement, le code SMS simulé est écrit dans le journal (`devOtpCode`) : c'est ainsi qu'on se connecte sans Twilio |
| 2026-09-25 | Revue de code de l'étape 10 : 10 constats corrigés | Créneaux calculés sur l'heure de Montréal (instants exacts groupés par jour de Montréal : le 1er novembre compte 25 heures, aucun jour répété ; un téléphone réglé sur un autre fuseau voit les heures de Montréal). Frais d'annulation annoncés par la règle du domaine (`clientCancellationFeeCents`) avec les réglages de l'API. File hors ligne : accès au stockage sérialisés et fusion en fin de rejeu (aucune écriture perdue), seules les requêtes non parties (`NETWORK_ERROR`) sont mises en file (après un délai dépassé, l'API a pu traiter la requête), 401, 408 et 429 gardés pour la passe suivante, `clear()` à la déconnexion. Champ d'adresse : corriger une adresse choisie ne vide plus le champ, une suggestion tardive n'écrase plus la dernière frappe. Devis : la réponse d'une demande dépassée est ignorée. Texto au passager tiers : une fois par course, jamais au client lui-même. Déconnexion et suppression : appareil retiré du compte (`DELETE /me/devices/{id}`), file vidée. Aucune règle écrite en dur dans l'application : `MAX_QUOTE_STOPS` du domaine, pourboires suggérés dans `/v1/config` (`rides.tip_suggestions_cents`, `rides.tip_max_cents`), frais d'annulation masqués tant que la configuration n'est pas lue ; `EXPO_PUBLIC_API_BASE_URL` dans `.env.example` |
| 2026-09-26 | Étape 11 avant les étapes 7, 8 et 9 : l'API chauffeur qui manquait est écrite ici | L'application chauffeur a besoin du dossier, de la formation, des revenus et des packs. Ajoutés : module `drivers` de l'API (candidature, profil, véhicules, documents, formation, compte de versement, accueil, revenus, relevés en lecture, packs, clients fidèles, tableau de conduite, fiche de course, paiement direct reçu, évaluation du client, incident, quart avec vérification faciale), migration 0006. Restent aux étapes 7 à 9 : Stripe réel (le compte Connect est simulé, `PaymentProvider.createConnectAccount`, `createConnectOnboardingLink`, `connectAccountStatus`), consommation des packs à la fin de course et report, génération des relevés et leur PDF |
| 2026-09-26 | Catégorie du véhicule déduite du modèle, jamais choisie par le chauffeur | Sélecteur des modèles admis (`vehicle_categories.allowed_models`), catégorie de rang le plus élevé dont le véhicule remplit le modèle, l'année minimale et les places (« Tesla Model X (7 places) » en XL, 5 places en Prestige). Refus expliqué : modèle absent, trop ancien, places insuffisantes. Véhicule en attente de validation jusqu'à l'inspection |
| 2026-09-26 | Documents téléversés en multipart, type reconnu à la signature | `POST /driver/documents` (champ `file`, 10 Mo, réglage `drivers.max_document_bytes`), JPEG, PNG, WEBP ou PDF reconnus aux premiers octets (l'extension et le type déclarés ne comptent pas), échéance exigée pour les documents qui expirent (`drivers.expiring_documents`), stockage objet par `STORAGE_PROVIDER` (simulé en mémoire en développement). Un document approuvé et valide reste la référence pendant l'examen de son remplaçant. Antivirus : étape 14 |
| 2026-09-26 | Formation Neomoov distincte de l'attestation SAAQ | Le type de document `training` reste l'attestation de formation reconnue par la SAAQ ; la formation Neomoov (4 modules bilingues dans `training.modules`, version de travail à valider par le fondateur, vidéos à produire) est suivie dans l'application, le quiz est corrigé par l'API (les réponses ne quittent jamais le serveur), l'attestation (`drivers.training_certified_at`) est exigée pour passer en ligne (`drivers.require_training`). Les chauffeurs déjà actifs sont réputés formés (migration 0006) |
| 2026-09-26 | Passage en ligne : trois prérequis de plus | Formation Neomoov réussie, compte de versement si `drivers.require_payout_account` (faux tant que Stripe n'est pas branché), consentement de géolocalisation non retiré (au moins un accord enregistré et aucun en vigueur ; un chauffeur qui n'a jamais répondu n'est pas bloqué ici). L'accueil affiche chaque raison et l'écran qui la règle |
| 2026-09-26 | Tableau de conduite calculé côté serveur, par jour | Accélérations et freinages brusques déduits des positions (`analyseDriving` du domaine : moyenne sur l'intervalle entre deux positions, 2,5 et 3 m/s² par défaut, réglage `driving.thresholds`, un épisode compte une fois), ponctualité des réservations (arrivée au plus 2 minutes après l'heure prévue), annulations du chauffeur. Une ligne par jour dans `driver_scores` (nouvelles colonnes : distance, courses, courses horodatées, courses ponctuelles), calculée une fois pour les jours passés et recalculée pour aujourd'hui ; agrégée sur 30 jours. Suggestions du domaine selon `driving.score_targets` |
| 2026-09-26 | Revenus : tarif du chauffeur, pourboires, frais d'annulation | Période du jour, de la semaine (lundi au dimanche, 5.8) ou du mois, heure de Montréal ; en paiement direct, `collectedDirectCents` rappelle ce que le chauffeur a déjà en poche (les frais de service et la redevance figurent au relevé). Le paiement direct reçu est confirmé à la fin de course (`POST /driver/rides/{id}/payment-received`) ; un écart avec le prix final ouvre un incident pour l'opérateur |
| 2026-09-26 | Localisation en arrière-plan : tâche du système, file locale, rien hors ligne | `expo-task-manager` et `expo-location` (service de premier plan Android, mode `location` iOS), statut gardé dans le trousseau lisible écran verrouillé (`createSecureStorage({ readableWhenLocked })`) pour la tâche relancée par le système, règle « 5 secondes ou 50 mètres » appliquée dans l'application et par l'API, file de 500 positions dans un fichier et envoi par lots de 100. Hors ligne : statut écrit d'abord, tâche arrêtée, file vidée, toute position livrée après coup ignorée (test automatisé). Sur le web, relevé toutes les 5 secondes et position datée à sa réception (le navigateur ne rappelle pas une position inchangée et resert l'ancienne date). Protocole de test sur appareil réel et mesure de batterie : `docs/testing/background-location.md` |
| 2026-09-26 | Offre : le compte à rebours suit l'échéance de l'API | 15 secondes pour une offre fixe ; une réservation planifiée avec véhicule ou favori demandé garde l'exclusivité plus longtemps (D37, `dispatch.favourite_exclusive_seconds`). L'offre montre maintenant la distance et la durée du trajet, les arrêts et les préférences du client |
| 2026-09-26 | Code commun des applications mobiles dans `mobile-core` | Mise en forme, téléphone, stockage sécurisé, session (`createSessionStore`, clé propre à chaque application) et éléments d'écran ; l'application client les réexporte. Outils des parcours web partagés dans `scripts/e2e/` (pilotage d'Edge, serveur statique qui résout groupes et segments dynamiques) ; le script du client garde ses propres copies jusqu'à sa prochaine exécution vérifiée |
| 2026-09-26 | Sentry reporté à l'étape 15 | Aucune clé Sentry disponible ; les rapports de plantage des deux applications seront branchés avec l'observabilité (étape 15) |
| 2026-09-26 | Revue de l'étape 11 : 9 constats corrigés | Activation d'un pack sous verrou par chauffeur (`pg_advisory_xact_lock`) : deux demandes simultanées ne créent ni deux packs actifs ni deux Découverte. Accueil : documents et packs lus une fois pour l'assistant et les alertes ; « planifiées à confirmer » limité aux courses à venir encore ouvertes. Application : rafraîchissement HTTP de repli seulement quand le socket est coupé (`useRealtimeStatus`), l'étape suivante de la course disponible dès la course relue (l'accueil se met à jour en arrière-plan), écran allumé par un crochet natif seulement (l'API web levait une erreur), libellés de tous les états de course, tarif du chauffeur dans la liste des courses, dates des périodes en toutes lettres, bandeau « ce client vous a choisi » (favori ou véhicule demandé). Parcours web : le chauffeur de démonstration quitte le service à la fin (il passait devant ceux des tests dans la liste limitée des véhicules libres) |
| 2026-09-26 | API de My Hub : une route par module, données sensibles masquées à la source | Module `admin` de l'API (tableau de bord agrégé, courses, chauffeurs et documents avec revue, véhicules, clients, incidents, approbations, paramètres typés, équipe, demandes de droits, prospects, packs, promotions, factures, relevés, agents, tarifs datés, zones, rapports et CSV), rôles `STAFF_READ_ROLES` en lecture et `STAFF_WRITE_ROLES` en écriture (paramètres : administrateur seul), toute écriture au journal d'audit avec l'acteur, lectures non journalisées. Masquage fait par l'API, jamais par l'interface : téléphone (`+1 514 •••-0123`), courriel, numéros de TPS et TVQ (4 derniers chiffres et suffixe du programme), numéros de documents. Migration 0007 : `staff_notes` (notes internes sur un chauffeur ou un client) et `leads` (prospects de l'API publique, consentement daté). Tarif : une ligne ne commence jamais dans le passé ; zone : polygone validé par le domaine (fermé, sans auto-intersection, bornes) puis enregistré |
| 2026-09-26 | My Hub : passerelle Next.js, jetons du personnel en témoins httpOnly | Le navigateur ne voit jamais un jeton du personnel. Les étapes de connexion passent par `/api/staff-auth/{étape}` (serveur web), qui pose trois témoins `SameSite=Strict` : accès (15 min), renouvellement (30 jours, chemin `/api` seulement) et profil (garde des pages `/hub`, sans valeur d'autorisation). Toutes les données passent par `/api/v1/*` : liste blanche (`admin/*`, `me`, `quotes`, `places`), jeton ajouté, renouvellement une fois sur 401 puis nouvel essai, session effacée si le renouvellement échoue ; toute écriture exige l'en-tête `x-neomoov-hub` (un site tiers ne peut pas l'ajouter sans accord CORS). Le socket `/admin` reçoit un jeton d'accès court redemandé à chaque connexion (`GET /api/session?socket=1`), jamais le jeton de renouvellement. Les rôles restent vérifiés par l'API : la passerelle n'élargit rien |
| 2026-09-26 | Connexion du personnel en deux facteurs sur le web | Courriel et mot de passe, puis code TOTP. Première connexion : QR (SVG produit par l'API) et clé à saisir, premier code, puis dix codes de secours affichés une seule fois ; connexion suivante par code ou par code de secours ; retour à l'étape du mot de passe si le jeton de passage a expiré. Déconnexion : jeton de renouvellement révoqué auprès de l'API, témoins effacés |
| 2026-09-26 | Interface de My Hub : composants maison au style shadcn/ui, WCAG 2.1 AA | Pas de dépendance Radix pour une quinzaine de composants (`components/ui/kit.tsx` : action, carte, badge, champ relié à son libellé et à son aide, tableau à en-têtes, pagination, dialogue natif `<dialog>` qui gère le focus et Échap). Texte blanc sur le bleu foncé de la charte (6,3:1) ; le bleu clair (3,9:1) n'est jamais un fond de texte. Lien d'évitement, navigation au clavier, combobox d'adresses au motif WAI-ARIA, graphique des rapports doublé d'un tableau pour les lecteurs d'écran. Textes FR-CA et EN dans `i18n-hub.ts` et `i18n-site.ts`, l'anglais typé par le français (une clé manquante ne compile pas) |
| 2026-09-26 | Cartes de My Hub et du suivi : Leaflet et tuiles OpenStreetMap en développement | Flotte (cercle par chauffeur, couleur selon l'état, filtres catégorie et état, positions en direct par `driver.location`), éditeur de zones (clics sur la carte, tracé fermé automatiquement, refus expliqué avant l'envoi par `validateRing` du domaine, revalidé par l'API) et position du chauffeur sur la page de suivi. Les tuiles publiques d'OpenStreetMap ne conviennent pas à un usage commercial soutenu : choisir un fournisseur de tuiles avant la mise en production (Google Maps, déjà payé, ou un fournisseur de tuiles OSM) |
| 2026-09-26 | Création d'une course par téléphone | L'opérateur tarife le trajet avec son propre compte (`POST /quotes` relayé, préavis de 2 heures appliqué par l'API), choisit la catégorie, puis crée la course pour un compte client existant (recherche) ou une fiche minimale (nom, téléphone, langue), payée au chauffeur. Attribution manuelle : recherche d'un chauffeur actif ; l'API vérifie la garantie de modèle. `GET /admin/rides/{id}/summary` ajouté : numéro public, client (téléphone masqué), chauffeur et prix, que la vue commune de la course ne porte pas |
| 2026-09-26 | Réservation web sans compte préalable | Devis affiché par l'API publique (clé `public:write` gardée par le serveur web, `NEOMOOV_PUBLIC_API_KEY`), adresses par `GET /v1/public/places/*` (ajoutés, 600 par heure et par adresse IP). Après la vérification du numéro par SMS (compte créé si besoin, conditions acceptées), le prix est recalculé avec le compte du client, mêmes règles, et l'écart éventuel est annoncé avant la création. Jetons du client en mémoire seulement (jamais de témoin ni de stockage : la page peut être dans une iframe d'un autre domaine). Paiement : au chauffeur seulement tant que Stripe n'est pas branché (étape 7) ; la carte est visible mais désactivée avec la raison. Confirmation avec le lien de suivi (`POST /rides/{id}/share`) |
| 2026-09-26 | Pages publiques : suivi, devenir chauffeur, droits | `/suivi/{jeton}` relaie `GET /v1/public/track/{jeton}` sans clé (route publique), rafraîchi toutes les 10 s pendant la course, jamais indexé ni transmis en référent. `/chauffeurs` : arguments, conditions et préinscription (prospect de l'API publique, consentement exigé, Turnstile : sans clé de site, jeton de développement accepté par l'API hors production seulement). `/droits` : vérification du numéro sans création de compte, demandes Loi 25 avec état, échéance et liens d'export |
| 2026-09-26 | En-têtes de sécurité du web | CSP sur toutes les pages : sources limitées à soi, à l'API (HTTP et WebSocket), aux tuiles de carte et à Turnstile ; `blob:` pour la visionneuse de documents ; `object-src 'none'`. `/reserver` : `frame-ancestors 'self'` et les domaines de `BOOKING_FRAME_ANCESTORS` (neomoov.net par défaut), sans `X-Frame-Options` ; ailleurs `frame-ancestors 'none'` et `X-Frame-Options: DENY`. HSTS en production. Limite connue : scripts en ligne permis (amorçage de Next.js sans nonce) ; passage aux nonces avec le durcissement de l'étape 14 |
| 2026-09-26 | Modules de My Hub livrés en lecture, en attente des étapes 7 à 9 et 13 | Packs et promotions (création et budgets : étape 8), relevés (génération, aperçu, émission, versements et prélèvements : étape 9), factures et état SEV (émission réelle : étape 9), agents IA (liste et file d'approbation actives ; branchement : étape 13). Le critère « générer et émettre un relevé » est reporté à l'étape 9. La lecture du fil de messages d'une course par l'opérateur n'a pas encore d'endpoint : ajouté avec les notifications (étape 13) |
| 2026-09-26 | Tests Playwright du web | `pnpm --filter @neomoov/web e2e` (`e2e/run.cjs`) démarre l'API compilée (journal lu pour les codes SMS simulés) et le web compilé sur le port 3100, crée trois comptes du personnel (préparation, administrateur, lecture seule) et une clé publique, lance Playwright (Edge, un seul travailleur), puis révoque la clé et retire les chauffeurs de test. Couverts : connexion 2FA (inscription, mauvais code, codes de secours, déconnexion), tableau de bord, course par téléphone et attribution, validation d'un chauffeur, tarif daté et simulation, refus d'un tracé de zone, décision d'un incident issu d'un SOS, approbation d'une action d'agent, factures, rapports et CSV, réservation web complète, suivi, préinscription, droits, en-têtes, rôle lecture seule, protection CSRF et routes non relayées. L'approbation d'agent est préparée en base (aucune API avant l'étape 13) ; les lignes de tarif de 2099 créées par le test sont supprimées à la fin. Captures dans `docs/screens/hub/` |
| 2026-09-26 | Revue de l'étape 12 : 11 constats corrigés | La garde des pages `/hub` exigeait le témoin de renouvellement, invisible hors du chemin `/api` : toute page renvoyait à la connexion (elle ne lit plus que le témoin de profil). Documents et approbations : l'API exige un état, le choix « Tous » (qui retombait sur « en attente ») est retiré. Réservation : le jeton d'accès du client (15 minutes) est renouvelé en mémoire si la saisie dure plus longtemps. Contrastes : bouton principal, langue active et accroche de l'accueil passés sur le bleu foncé. Étapes de connexion protégées par le même en-tête anti-CSRF que les écritures. Sous-composants des rapports sortis du composant de page (recréés à chaque rendu). Chronologie, acteurs et états des offres traduits (codes bruts auparavant), code en repli pour un événement inconnu. Composant partagé exporté par une page (refusé par Next.js) déplacé dans `components/hub/`. Suivi relayé sans clé publique : il fonctionne même sans `NEOMOOV_PUBLIC_API_KEY`. File de revue des documents et compteur du tableau de bord : chauffeurs sortis du service exclus (leur fiche garde tous leurs documents). Tests de l'API : le chauffeur « en ligne » d'un fichier reçoit aussi les offres des courses des autres fichiers ; ces offres et attributions planifiées sont supprimées au nettoyage (elles bloquaient la suppression de tout le lot d'utilisateurs, et les chauffeurs restés actifs faisaient sortir un véhicule de la liste limitée du test de répartition) |
| 2026-09-26 | Étape 7 faite après l'étape 12, avec le fournisseur simulé | Les clés Stripe ne sont pas encore fournies. L'adaptateur réel est livré complet par l'API REST de Stripe (sans SDK : moins de dépendances, corps en formulaire, version d'API figée `2024-06-20`, idempotence sur chaque écriture, signature des webhooks vérifiée en temps constant avec 5 minutes de tolérance), testé sans réseau par un faux `fetch` ; les tests contre Stripe en mode test (`test/stripe.live.test.ts`) attendent `RUN_STRIPE_TESTS=1` et une clé `sk_test_…`. La clé secrète est un champ privé JavaScript : absente de `JSON.stringify` comme de `util.inspect`. Passage au réel : `docs/paiements.md` |
| 2026-09-26 | Autorisation : prix maximal consenti + 15 %, 20 $ au plus ; quand l'autoriser | Règle du domaine (`authorizationCents`, réglages `payments.authorization_margin_*`). Course immédiate par carte : autorisée avant sa création (un refus empêche la demande, 402 `PAYMENT_DECLINED` ; une authentification bancaire demandée, 402 `PAYMENT_AUTHENTICATION_REQUIRED` avec le secret client) ; si la création échoue ensuite (devis déjà utilisé), l'autorisation est levée, jamais sur un rejeu de la même clé. Réservation : carte vérifiée et choisie à la réservation, autorisée à l'attribution ; une autorisation Stripe expirant après 7 jours, elle est différée tant que la prise en charge est à plus de 6 jours (`payments.authorization_lead_days`) puis faite par la reprise périodique (worker toutes les 5 minutes, API sans Redis hors tests). Une carte refusée à l'attribution : paiement en échec, incident `payment_failed`, avis au client |
| 2026-09-26 | Capture, frais et levée de l'autorisation par la file `payments` | Abonnements aux événements de course (`ride.assigned`, `ride.completed`, `ride.cancelled_by_client`, `ride.no_show`, `ride.no_driver`, `ride.cancelled_by_driver`) : chaque réaction est une tâche d'identifiant stable (avec Redis, plusieurs processus reçoivent le même événement, BullMQ n'en garde qu'une). Fin de course : capture du prix final moins les crédits, jamais au-delà de l'autorisation (un dépassement devient un solde dû). Annulation ou absence avec frais : capture des frais sur l'autorisation (nature `cancellation_fee` ou `no_show_fee`), sinon autorisation levée. Paiement direct au chauffeur : aucune autorisation ; les frais d'annulation d'une course payée au chauffeur ne sont pas encaissés en V1 (journalisés sur la course) |
| 2026-09-26 | Échec de capture : nouvelle tentative, puis ticket et solde dû | `payments.capture_attempts` (2) tentatives, une clé d'idempotence par tentative (Stripe mémorise aussi les refus). Après la dernière : paiement `failed`, incident `payment_failed` pour l'agent relation client, solde dû sur le client (`clients.balance_due_cents`), avis au client ; toute nouvelle réservation est refusée (402 `BALANCE_DUE`) jusqu'au règlement par `POST /v1/me/settle` (un paiement hors session par course impayée, clé `balance:<paiement>`, rejouable sans double débit). Le solde est lu avec le profil client, sans requête de plus avant la répartition |
| 2026-09-26 | Cartes : SetupIntent confirmé par l'application, relu par l'API | `POST /payment-methods/setup-intent` (clé publiable, identifiant marchand Apple Pay, pays), la feuille de paiement Stripe confirme, puis `POST /payment-methods/confirm` : l'API relit le SetupIntent chez Stripe, vérifie qu'il appartient au client Stripe de l'utilisateur, et n'enregistre que la marque, les 4 derniers chiffres et l'échéance. Une carte connue n'est jamais reprise pour un autre compte. Carte par défaut, retrait (détachée chez Stripe, une autre devient la carte par défaut). Même mécanisme pour la méthode de prélèvement du chauffeur (colonnes de migration 0009). Le reçu d'une course montre la carte masquée au client, jamais au chauffeur |
| 2026-09-26 | Pourboire, paiement direct, remboursements | Pourboire : paiement séparé hors session sur la carte de la course ou la carte par défaut, une fois par course (clé `tip:<course>`), plafonné (`rides.tip_max_cents`) ; l'évaluation avec pourboire le débite avant d'enregistrer la note (un refus n'enregistre rien). Paiement direct : `paidDirect` dans la fin de course (ou `payment-received` après coup), un seul enregistrement dans le module des paiements, écart = incident. Remboursement (`POST /v1/admin/rides/{id}/refund`, administrateur, opérateur ou finances) : sur la carte dans la limite du capturé moins les remboursements, ou crédit sur le compte (`credits.origin = refund`) plafonné au prix de la course ; seule voie pour un paiement direct ; motif obligatoire ; idempotent (en-tête facultatif, sinon clé tirée du montant, du motif et du mode). Les plafonds des agents (50 $) arrivent avec l'étape 13 |
| 2026-09-26 | Webhooks idempotents et reprise | `POST /v1/webhooks/stripe` (public, corps brut conservé par `createApp`) : signature vérifiée, événement enregistré par son identifiant dans `webhook_events` (un même événement reçu trois fois : une écriture, un traitement), réponse immédiate, traitement par la file. Un traitement en échec est repris (`payments.webhook_max_attempts`, 5) par la reprise périodique ou `POST /v1/admin/payments/webhooks/retry`. Les litiges ouvrent un incident ; les transferts et versements sont journalisés pour le rapprochement de l'étape 9 |
| 2026-09-26 | Modes de paiement proposés dans le devis | `paymentMethods` dans la réponse de devis : carte, Apple Pay et Google Pay toujours ; espèces, Interac et terminal seulement si un chauffeur actif les accepte (en ligne pour une immédiate, acceptant les réservations pour une planifiée). Une seule ville en V1 : la zone est la ville. Le prépaiement Interac et le paiement échelonné (5.6) restent à faire : aucune autorisation n'est créée pour eux |
| 2026-09-26 | Tests : carte simulée par défaut, identifiants simulés uniques par processus | Une course prépayée par carte exige maintenant une carte : l'aide `loginByOtp` enregistre une carte simulée (`{ card: false }` pour un compte sans carte). Les fichiers de tests tournent dans des processus séparés sur la même base : les identifiants du fournisseur simulé portent un préfixe propre au processus (sinon deux fichiers produisaient la même carte `pm_mock_000001`). 11 scénarios d'intégration dans `test/payments.e2e.test.ts` (chaque opération financière rejouée : confirmation de carte, création, capture, pourboire, règlement, remboursement, webhook trois fois) ; tableau des états par scénario dans `docs/paiements.md` |
| 2026-09-26 | Applications et web : écrans de paiement à brancher | Le client d'API expose `api.payments` (cartes, pourboire, reçu, solde, règlement, Connect). Restent à brancher, avec la clé publiable Stripe : la feuille de paiement du SDK Stripe React Native dans l'application client (moyens de paiement, prépaiement), Stripe Elements dans la réservation web (aujourd'hui paiement au chauffeur seulement), l'écran de prélèvement de l'application chauffeur. My Hub affiche les paiements d'une course et permet le remboursement ou le crédit |
| 2026-09-26 | Positions : `flush` attend l'écriture déjà partie | Défaut trouvé par un test de charge instable : l'écriture périodique des positions prenait le tampon, et un appel à `flush` pendant qu'elle attendait la base trouvait le tampon vide et rendait la main tout de suite. La fin de course, qui appelle `flush` avant de reconstituer le trajet, pouvait donc manquer les dernières positions. Chaque `flush` attend maintenant l'écriture en cours (les écritures restent dans l'ordre) |
| 2026-09-26 | Étape 8 développée en trois branches parallèles | Promotions et packs dans `etape-08-packs-promotions` ; crédits et parrainage (`etape-08b-credits-parrainage`) et favoris et garantie modèle (`etape-08c-favoris-garantie`) développés par des sous-agents dans des copies de travail séparées (`git worktree`), puis fusionnés dans la branche de l'étape. `openapi.json` régénéré une seule fois après les fusions (fichier généré, source de conflits) |
| 2026-09-26 | Promotions : règles en données, code saisi puis promotions automatiques | Le moteur du domaine (`evaluatePromotion`) lit la ligne `promotions` (type `percent`, `fixed`, `free_ride`, `nth_ride` ; conditions `firstRideOnly`, `maxDistanceMeters`, `categories`, `zones`, `timeWindow`, `maxClients`, `autoApply` ; limites globale et par client, budget, validité) et l'usage réel (`promotion_uses`). Au devis : le code saisi s'il est admissible, sinon la première promotion automatique admissible (troisième et dixième course : `autoApply`), jamais pour un devis public ou fait par le personnel. Un code refusé pour sa catégorie laisse les autres catégories du devis ; tout autre refus fait échouer le devis avec un motif stable (`PROMOTION_NOT_APPLICABLE`, `details.reason`). `POST /v1/promotions/validate` donne le même motif, et la remise exacte sur un devis du client |
| 2026-09-26 | Promotions : usage réservé à la création, rendu si la course n'aboutit pas, prix garanti à la fin | L'usage (`promotion_uses`, relié à la course) et la dépense du budget sont écrits dans la transaction qui crée la course ; ils sont rendus sur `ride.cancelled_by_client`, `ride.no_show` et `ride.no_driver`. Pas de revalidation à la fin de course : la remise promise au devis est gardée (prix fixe garanti). Compensation du chauffeur : 100 % de la remise (`driver_compensation_cents`), portée à son relevé à l'étape 9 ; le chauffeur ne finance jamais une promotion |
| 2026-09-26 | Packs : consommation à la fin de course, une fois par course | `PackLifecycleService` (module des courses, pour être chargé par le worker) écoute `ride.completed` : verrou du chauffeur (le même que l'activation), une ligne `pack_consumptions` par course (index unique : plusieurs processus peuvent recevoir l'événement), courses reportées consommées d'abord, Illimité sans décrément. Alerte `pack.low` quand il reste exactement `packs.low_threshold` (3) courses. À l'épuisement : renouvellement immédiat avec le pack demandé en changement (`nextPackCode`) ou le même, facturé au relevé suivant ; l'option passe au nouveau pack (l'ancien ne se renouvelle jamais deux fois). Découverte ne se renouvelle pas (une seule fois par chauffeur) : avis `pack.renewal_failed` |
| 2026-09-26 | Packs : passe horaire d'expiration, de renouvellement et de report | File `packs` : le worker la porte avec Redis, l'API sans Redis (hors tests, qui l'appellent directement). Packs échus passés en `expired` (avis `pack.expired`), Illimité et packs à renouvellement automatique renouvelés, courses non utilisées reportées une seule fois sur le premier pack activé après l'échéance et dans les 7 jours (`packs.rollover_window_days`, colonne `rollover_allowed` du catalogue respectée) ; passé ce délai, le report est clos. Une activation manuelle applique aussitôt le report et met fin aux renouvellements en attente des packs échus. Horaire plutôt que quotidien : un pack échu ne laisse jamais un chauffeur une journée sans offres |
| 2026-09-26 | Packs : un seul motif de refus des offres, trois valeurs | Même règle dans le domaine (`canReceiveOffers`), au passage en ligne et dans la sélection SQL des chauffeurs : pack actif non échu avec des courses (reportées comprises) ou Illimité, ou renouvellement automatique en attente. Motifs `pack_required`, `pack_exhausted`, `pack_expired` dans `status.update` (quand `drivers.require_active_pack` est actif), traduits dans l'application chauffeur avec un lien vers les packs |
| 2026-09-26 | Découverte offert aux locataires R-LuxeEV : indicateur posé dans My Hub | `drivers.is_rluxe_ev_tenant` (migration 0010), modifiable par `POST /v1/admin/drivers/{id}/programs` (personnel en écriture, journalisé) et une case de la fiche chauffeur ; lu par l'activation et le catalogue des packs. Rang des 100 premiers chauffeurs : ordre d'inscription parmi tous les comptes chauffeurs |
| 2026-09-26 | Crédits : consommés à la fin de course, le plus proche de son expiration d'abord | Le devis déduit les crédits disponibles et la course garde le montant (`credits_applied_cents`) ; à la fin, ce montant est prélevé sur les crédits valables à la réservation, par expiration la plus proche (sans expiration en dernier), une ligne `credit_uses` par crédit entamé, sous verrou des crédits du client (rejouable). Un manque (crédits utilisés ailleurs entre-temps) est journalisé, sans incident. Validité : `credits.validity_days` (365) pour tout crédit, remboursement en crédit compris. Réaction directe à l'événement avec deux nouvelles tentatives (pas de file) |
| 2026-09-26 | Parrainage client et chauffeur | Code personnel de 6 caractères sans ambiguïté (sans 0, O, 1, I, L), lien de partage (`referral.link_base_url`), créés au premier appel de `GET /v1/me/referral`. Client : code saisi à l'inscription et avant la première course (`POST /v1/me/referral/apply`), crédits du parrain et du filleul (`referral.client_*`) après la première course terminée du filleul. Chauffeur : code donné à la candidature (un code inconnu répond 404), crédit de pack de 50 $ (`referral.driver_referrer_cents`) au parrain après 50 courses du filleul (`referral.driver_threshold_rides`). Refus : son propre code, parrainage croisé (`REFERRAL_RECIPROCAL`), code client pour un chauffeur (`REFERRAL_NOT_A_DRIVER`), après la fenêtre de saisie |
| 2026-09-26 | Crédit de pack : origine à part, jamais déduit d'une course | Migration 0011 : origine `driver_pack`. Le crédit de pack d'un chauffeur est listé par `GET /v1/me/credits` mais exclu du disponible des devis et de la consommation des courses ; il sera déduit de ses packs au relevé (étape 9) |
| 2026-09-26 | Favoris : après une course notée 4 ou plus | `POST/DELETE /v1/me/favorites/{driverId}` (idempotents), `GET /v1/me/favorites` ; ajout refusé (409 `FAVOURITE_NOT_ELIGIBLE`) sans course terminée avec ce chauffeur notée au moins `favorites.min_rating` (4). Les deux lectures de la répartition (`favorite_drivers` et `client_driver_links.favorite_since`) restent cohérentes. Devis : un chauffeur qui n'est pas un favori du client, 400 `FAVOURITE_NOT_ALLOWED` ; favori indisponible (inactif, hors ligne pour une immédiate, n'acceptant pas les réservations pour une planifiée) : supplément retiré et `favouriteDriver` dans `ignoredOptions`, la répartition peut encore le préférer. Limite connue : la disponibilité ne vérifie pas les conflits d'horaire à la date demandée |
| 2026-09-26 | Garantie modèle : décision propre, remboursement intégral | `POST /v1/admin/incidents/{id}/guarantee` (administrateur ou opérateur), distincte de la décision générique d'un incident. Validée : remboursement de ce que le client a payé pour la course (capturé sur la carte, ou montant confirmé par le chauffeur plafonné au prix, en crédit d'origine `guarantee` pour un paiement direct), clé `guarantee-<incident>` rejouable ; tarif normal maintenu au chauffeur quand la faute n'est pas la sienne (`rides.driver_fare_protected`, lu par le relevé de l'étape 9), sinon sanction proposée par une note interne, jamais appliquée automatiquement. Refusée : clôture motivée. Crédits utilisés et pourboire non rendus ; paiement en échec (solde dû) refusé en 409. Fenêtre « Garantie modèle » dans les incidents de My Hub |
| 2026-09-26 | Rien à payer : aucune autorisation, aucune capture nulle | Une course entièrement couverte (crédits, course offerte) n'est pas autorisée à la création ou à l'attribution ; à la fin, l'autorisation éventuelle est levée et la ligne de paiement close (`payment_nothing_due`). Stripe refuse une capture nulle |
| 2026-09-26 | Registres de la redevance et des taxes : une ligne par course terminée, période du mois de Montréal | Sur `ride.completed`, tâche d'identifiant stable dans la file `exports` (worker avec Redis, API sans Redis) : `redevance_ledger` et `tax_ledger` écrits avec `ON CONFLICT DO NOTHING` sur les index uniques par course (rejouable, plusieurs processus). Taux et redevance lus dans les règles de tarification de la ville ; taxes par `splitTaxDetail` ; période = mois local de la fin de course (`service.time_zone`). Passe horaire de reprise des courses terminées sans ligne (500 par passe, jamais en test : les tests la limitent à leurs courses). Détails : `docs/registres-exports.md` |
| 2026-09-26 | Redevance due même sur une course offerte | La course offerte (`waivesFees`) ne facture pas la redevance au client ; le registre inscrit quand même le montant par course en vigueur (`pricing.regulatory_fee_cents`), puisque la redevance est due par course terminée : l'écart est à la charge de Neomoov. Le CSV et le rapport montrent la redevance due et la redevance facturée. **À confirmer par le comptable** avec la question D2 |
| 2026-09-26 | Exports des registres : `AAAA-MM` ou `AAAA-Tn`, CSV en cents avec totaux, accès finances et administrateurs | Trimestres civils notés `2026-T3` (déclarations trimestrielles de TPS et TVQ). `GET /v1/admin/ledgers/exports` : point-virgule, cents, une ligne par course, `TOTAL AAAA-MM` par mois (même vide) et `TOTAL AAAA-Tn`. Routes réservées à `STAFF_FINANCE_ROLES` (finances, administrateur), téléchargements journalisés. Remise de la redevance par mois terminé seulement, date à midi heure de Montréal, référence au journal d'audit ; une ligne inscrite après la remise reste à remettre |
| 2026-09-26 | Rapport de synthèse PDF : état dans le stockage objet, sans table | Le worker produit le PDF (file `exports`) ; son état (`pending`, `ready`, `failed`) est un manifeste JSON à côté du PDF (`ledgers/summaries/<type>/<période>.json`), lu par l'API et le worker : pas de migration. Une table `ledger_reports` (demandeur, dates, clé, erreur) serait plus propre si l'historique des rapports devient utile |
| 2026-09-26 | Export de géolocalisation : format provisoire `csv-v0`, pseudonymes HMAC, coordonnées à 3 décimales | Décrit dans `docs/geolocation-export.md`, à confirmer avec la CTQ. Pseudonymes HMAC-SHA256 (128 bits) avec une clé dérivée par HKDF de `ENCRYPTION_KEY` et l'étiquette `neomoov/geolocation-export/v1` ; aucun identifiant réel dans le fichier. Passe horaire : export du mois précédent dès le 1er (rattrapage si le worker était arrêté) ; lancement manuel pour un mois terminé, qui remplace le fichier de la même ligne (l'ancien reste archivé), jamais un export transmis ; verrou consultatif contre deux productions simultanées |
| 2026-09-26 | Facturation : document figé dans `invoices.lines`, sans migration | La colonne `lines` (JSON) porte tout le contenu de la facture à l'émission (document de version 1, schéma `invoiceDocumentSchema`) : fournisseur et ses numéros, Neomoov (`company.*`), client (prénom et initiale), trajet, lignes libellées par partie (`driver` : transport ; `platform` : frais de service, redevance, péages), taxes perçues et par nature, paiement, mention légale, composantes remboursables, et pour une note de crédit le remboursement d'origine. Une facture reste identique même si la course, le chauffeur ou les réglages changent (conservation 7 ans) ; seuls l'état SEV, la transaction et la clé du PDF évoluent. L'unicité d'une note de crédit par remboursement tient à un verrou consultatif et à une relecture (`lines->'creditNote'->>'refundId'`) : un index unique demanderait une colonne `refund_id` (migration à envisager plus tard) |
| 2026-09-26 | Numérotation sans trou : compteurs dans la transaction d'insertion, verrous toujours dans le même ordre | Verrou consultatif de l'émission (course ou remboursement), relecture, `next_invoice_number()` puis `next_invoice_supplier_sequence(chauffeur)` en deux requêtes (global puis fournisseur : jamais d'interblocage), insertion. Une émission concurrente de la même course attend puis retrouve la facture ; une violation de `invoices_ride_unique` annule tout, numéros compris. Les notes de crédit prennent aussi un numéro global et la séquence du chauffeur. Test : 100 courses d'un chauffeur émises en parallèle, 20 deux fois : séquences 1 à 100, numéros globaux croissants, aucun doublon |
| 2026-09-26 | Contenu de la facture d'une course | Lignes du transport : composantes du devis (`quotes.lines`), attente (`wait_time`), ajustement jusqu'au tarif final (prix négocié, supplément favori retiré), promotion en négatif ; Neomoov : frais de service facturés (après remise d'alignement), redevance, péages. TPS et TVQ perçues, et leur détail par nature par `splitTaxDetail` (taxes du chauffeur sur le tarif complet, taxes de Neomoov sur les frais et les péages) ; après une promotion, l'écart pris en charge par Neomoov est affiché. Total = prix final + pourboire connu à l'émission (un pourboire donné plus tard n'est pas ajouté : la facture est immuable). Un écart d'arrondi éventuel est compensé par une ligne `rounding` et journalisé. **À confirmer avec le comptable** : péages dans la part de Neomoov, redevance dans l'assiette des taxes |
| 2026-09-26 | Frais d'annulation et de non-présentation : facture du chauffeur, sans taxe en V1 | Facture `cancellation` ou `no_show` dès que des frais sont facturés (montant enregistré sur la course), une ligne au chauffeur (100 % des frais lui reviennent), aucune taxe ajoutée : le traitement fiscal (indemnité ou fourniture taxable) reste **à confirmer avec le comptable** (docs/sev-adapter.md). Facture émise aussi pour une course payée au chauffeur, dont les frais ne sont pas encaissés en V1 |
| 2026-09-26 | Notes de crédit : une par remboursement, au prorata du reste de la facture | Événement de domaine `payment.refunded` émis par le module des paiements pour un remboursement réussi (carte ou crédit, garantie modèle comprise) ; un remboursement encore en attente chez Stripe l'émet quand le webhook le confirme (`refund.updated`, `charge.refunded`). Limite connue : un remboursement qui échoue chez Stripe après avoir réussi garde sa note (correction par une nouvelle facture, à trancher avec le comptable). La note répartit le montant rendu au prorata de ce qui reste à créditer (transport payé, frais, redevance, péages, TPS et TVQ perçues par nature), au cent près : la somme des notes ne dépasse jamais la facture, la dernière solde exactement. Montants positifs, `credit_note_of_id` vers la facture d'origine, émise d'abord si elle manquait |
| 2026-09-26 | SEV : transmission par la file `invoicing`, réservation, tentatives et reprise | Réservation de la facture (état `sent`) pour un seul processus à la fois, une ligne `sev_transmissions` par tentative, délai de 15 s par appel. Échec : `pending`, puis `error` après `sev.max_attempts` (5). Passe périodique (5 min, worker avec Redis, API sans Redis hors tests) : `pending` après `sev.retry_delay_seconds` (60), `error` après `sev.error_retry_seconds` (3 600), transmission interrompue libérée après 10 min, factures manquantes des courses des `invoices.catchup_days` (2) derniers jours, PDF manquants. Note de crédit transmise après sa facture d'origine (la file transmet l'origine d'abord et refait son PDF ; la passe ignore les notes dont l'origine n'a pas de transaction). Chaque élément de la passe est isolé : une facture en échec est journalisée sans arrêter les autres. Reprise manuelle `POST /v1/admin/sev/retry/{invoiceId}` (administrateur, opérateur, finances ; 409 si déjà accusée ou en cours) ; état `GET /v1/admin/sev/status` (compteurs, 20 dernières erreurs, `healthcheck`). Contrat de l'adaptateur réel et champs à confirmer : `docs/sev-adapter.md` |
| 2026-09-26 | Identifiants des tâches BullMQ sans deux-points | BullMQ 6 refuse un identifiant personnalisé contenant « : » (sauf la forme à trois parties des répétitions) : la file `invoicing` utilise `ride-<id>`, `refund-<id>`, `transmit-<id>-<horodatage>`. **Défaut constaté dans la file `payments`** (`completed:<id>`, `fee:<id>`, `webhook:<id>`…) : avec Redis, ces tâches seraient refusées à la mise en file ; à corriger après la fusion (sans effet en mode mémoire) |
| 2026-09-26 | Vérification publique d'une facture et PDF | Code QR : `invoices.verification_base_url?t=<jeton>`, jeton de 44 caractères (version, identifiant, 16 octets de HMAC-SHA256) signé avec une clé dérivée par HKDF-SHA256 de `ENCRYPTION_KEY` sous l'étiquette `neomoov/invoice-verification/v1` (jamais la clé brute). `GET /v1/public/invoices/verify/{token}` (publique, comme le suivi partagé ; relayée sans clé par le web) rend le minimum : numéro, nature, date, fournisseur, total, état SEV ; jeton altéré : 404. Page `/verifier-facture` du web. PDF (format lettre, français, pdfkit) produit par la file, rangé par l'adaptateur de stockage sous `invoices/AAAA/MM/<numéro>.pdf`, refait après l'accusé du SEV pour y porter la transaction ; premier rendu : courriel `invoice.issued` en file (clients avec compte ; envoi réel à l'étape 13). Exemples fictifs dans `docs/examples/` |
| 2026-09-26 | Étape 9 développée en trois branches parallèles sur un socle commun | Socle `07f73c4` : migration 0012 (`invoices.kind` : course, annulation, non-présentation, note de crédit), `splitTaxDetail` du domaine (TPS et TVQ du tarif pour le chauffeur, des frais pour Neomoov, même règle que `splitTaxes`), réglages de facturation (`company.*` à confirmer avec le comptable, `invoices.legal_notice`, `invoices.verification_base_url`, `sev.max_attempts`, `geolocation_export.format`, `settlement.retry_weekday`). Relevés, versements et prélèvements dans `etape-09-reglement` ; factures et SEV (`etape-09b-facturation`) et registres, exports et géolocalisation (`etape-09c-registres`) par des sous-agents |
| 2026-09-26 | Relevés : une course ou un pack sur un seul relevé, les retards passent au suivant | Les lignes viennent des courses terminées (ou annulées avec frais) et des packs `to_bill` jusqu'à la fin de la période, 8 semaines en arrière au plus, et qui ne figurent sur aucun autre relevé : une course dont l'événement arrive après l'émission va au relevé suivant, jamais deux fois. Verrou par chauffeur pendant la génération. Tous les montants viennent du moteur du domaine (`classifyRideForStatement`, `packBillingLines`, `buildStatement`) ; les colonnes agrégées de `weekly_statements` en sont la somme par nature. Paiement direct = `payment_choice = pay_driver_after` ; le pourboire passe toujours par la plateforme (un pourboire en espèces n'est pas connu) |
| 2026-09-26 | Brouillon, aperçu, émission immuable, ajustement sur le brouillon | `POST /v1/admin/statements/generate` (lundi donné, sinon la dernière semaine écoulée ; `preview` calcule sans rien écrire) crée ou recalcule les brouillons en gardant leurs ajustements manuels, sans jamais toucher un relevé émis. `issue` fige le relevé : packs portés passés à `billed` (avec le relevé), crédit de pack prélevé, courriel `statement.issued`, événement `statement.issued` qui déclenche le PDF. Un ajustement motivé (`adjust`, audité) ne s'ajoute qu'à un brouillon ; un relevé émis se corrige sur le brouillon de la semaine suivante. Finances et administration en écriture, tout le personnel en lecture |
| 2026-09-26 | Crédit de pack du parrainage : déduit des packs du relevé | Ligne `referral_credit` égale au plus petit du crédit `driver_pack` disponible et des packs facturés sur ce relevé (prix et taxes) ; le reste attend le relevé suivant. Le crédit est prélevé à l'émission (le plus proche de son expiration d'abord) |
| 2026-09-26 | Garantie modèle, chauffeur en faute : tarif retiré au relevé | Course dont la garantie est validée sans tarif protégé : ligne `adjustment_negative` égale au tarif et à ses taxes (ce que la course lui a crédité), que le client ait payé par carte ou directement. Chauffeur sans faute (`driver_fare_protected`) : rien ne change, Neomoov absorbe le remboursement |
| 2026-09-26 | Frais d'annulation dus : pas encore de source | La nature `cancellation_fee_due` du domaine (frais qu'un chauffeur devrait à Neomoov) n'a aucune donnée qui l'alimente en V1 : les frais d'une course payée au chauffeur ne sont pas encaissés (décision de l'étape 7). À brancher si une règle de pénalité chauffeur est décidée |
| 2026-09-26 | Versement, prélèvement, nouvel essai, suspension | Net positif : transfert Connect (clé `statement:<id>:payout`, un compte Connect vérifié exigé, sinon échec `payout_account_missing` sans suspension). Net négatif : prélèvement hors session sur la méthode enregistrée (clé par tentative ; sans méthode, échec `debit_method_missing`). Net nul : réglé sans mouvement. Échec : relevé `failed`, avis au chauffeur, solde (`driver_balances`) = somme des relevés non réglés. Nouvel essai automatique le lundi (`settlement.retry_weekday`, 20 heures au moins après le précédent) ; suspension pour solde (`suspended_for_balance_at`, hors ligne aussitôt, avis) quand la règle du domaine le dit : seuil de 150 $ dépassé après ce nouvel essai, ou impayé depuis plus de 7 jours ; réactivation automatique dès que plus rien n'est dû. `POST /v1/admin/statements/{id}/pay` relance à la main, rejouable |
| 2026-09-26 | File `settlements` : passe du quart d'heure | Le vendredi à partir de 6 h (heure de Montréal, `settlement.generation_*`) : génération de la semaine précédente, émission de ses brouillons, règlement ; le lundi : nouveaux essais ; chaque passe : revue des soldes (délai de 7 jours). Chaque étape est rejouable. Le worker porte la file avec Redis ; sans Redis, l'API (hors tests, qui appellent la passe avec une horloge simulée) |
| 2026-09-26 | PDF du relevé : produit par la file, lien signé pour le chauffeur | pdfkit, police standard (signe moins ASCII, séparateur des milliers en espace simple : la police WinAnsi n'a ni le signe moins typographique ni l'espace fine), chaque course liée à sa fiche dans l'application chauffeur (`neomoov-driver://ride/<id>`). Stocké par l'adaptateur de stockage ; My Hub le lit par la passerelle, le chauffeur reçoit un lien signé de 10 minutes dans le détail du relevé (ouvert dans le navigateur par l'application). Exemple : `docs/examples/releve-exemple.pdf` et `.json` (`pnpm --filter @neomoov/api example:statement`) |
| 2026-09-26 | Identifiants de tâche normalisés par la file | Correctif central de `QueueService.add` : tout « : » d'un identifiant de tâche devient « - » avant BullMQ (qui le refuse hors trois segments), pour toutes les files, y compris celles des paiements de l'étape 7 qui auraient échoué dès l'arrivée de Redis. Les appelants gardent des identifiants lisibles |
| 2026-09-26 | Agents IA : modèle `claude-opus-5-5` par défaut, effort toujours envoyé | Consigne de l'étape 13 : le cahier nomme `claude-opus-5`. D'après la documentation du SDK officiel, `claude-opus-5` est bien un identifiant publié (Claude Opus 5) ; `claude-opus-5-5` (Claude Opus 5.5) lui succède, moins cher (4 $ et 20 $ US par million de jetons en entrée et en sortie, contre 5 $ et 25 $), avec le même contexte. Différences prises en compte : raisonnement toujours actif (l'effort est le seul réglage, défaut `medium` côté API : l'effort de l'agent est donc toujours envoyé), `tool_choice` forcé refusé (jamais utilisé), classificateurs de sécurité plus larges (refus traduit en erreur typée `refused`). Colonne `agents.model` : défaut `claude-opus-5-5`, agents existants basculés par la migration 0013 ; effort `low` à `max` (contrainte élargie). Un retour à `claude-opus-5` se fait par agent dans My Hub |
| 2026-09-26 | SDK Anthropic 0.128 : requêtes par l'espace `beta`, repli côté serveur réglable | `@anthropic-ai/sdk` 0.128.0 (dernière version, signatures vérifiées dans le paquet). Le repli côté serveur (`fallbacks: "default"`, en-tête `server-side-fallback-2026-07-01`) n'existe que sur `client.beta.messages` : les sorties structurées passent par `beta.messages.parse` avec `betaZodOutputFormat` (équivalent bêta de `messages.parse` et `zodOutputFormat`), la boucle d'outils par `beta.messages.toolRunner` et `betaZodTool`. Raisonnement adaptatif, prompt système mis en cache (`cache_control` éphémère ; en dessous de 512 jetons il n'est simplement pas mis en cache), 2 nouvelles tentatives et 120 s de délai par requête. Les cibles de repli de Claude Opus 5.5 ne sont pas publiées : si l'API refusait le paramètre, `LLM_SERVER_FALLBACK=off` le retire sans changer le code. Erreurs typées du SDK traduites en `LlmError` (`rate_limited`, `unavailable` : reprise possible ; `authentication`, `bad_request`, `invalid_output`, `refused`, `truncated`), jamais le message brut de l'API. Clé dans un champ privé, `toJSON` sans la clé (test) |
| 2026-09-26 | Exécuteur des agents : journal, coût, plafond de dépense, idempotence | Une exécution = une ligne `agent_runs` : entrées minimisées (identifiants, longueurs, jamais le texte d'un client), sortie, appels d'outils (entrée et résultat minimisés, durée, approbation), jetons dont cache en lecture et en écriture, modèle qui a servi (repli compris), coût en micro-dollars US selon le barème `agents.llm_pricing` (arrondi au supérieur), durée, statut (`skipped` ajouté : mode manuel, agent inactif ou plafond). Plafond quotidien (heure de Montréal) : le plus bas de `agents.daily_budget_micros` (20 $ pour l'ensemble des agents) et du seuil `dailyBudgetMicros` de l'agent ; vérifié avant et après chaque exécution, il fait passer l'agent en `manual` (journal d'audit, alerte au personnel) ; le fondateur le remet en service dans My Hub. Un déclencheur porteur d'une référence (message, document, relevé, période d'un rapport) ne s'exécute qu'une fois (index unique partiel) ; une exécution en échec est reprise par le même déclencheur |
| 2026-09-26 | Agents V1 : relation client en boucle d'outils, les trois autres en enchaînements fixes | Relation client : accusé de réception envoyé avant tout appel au modèle, classification structurée (catégorie, langue, plainte de sécurité, ton hostile), escalade immédiate sans outil sur plainte de sécurité (incident élevé) ou ton hostile, sinon boucle d'outils (courses et compte du client de la conversation seulement, remboursement, crédit, incident, escalade) ; la réponse finale du modèle est envoyée par le code (l'outil `sendMessage` n'est pas présenté au modèle, pour éviter un double envoi ; il reste exposé aux autres agents par la route interne). Recrutement, comptabilité et analyse : étapes fixes (outils journalisés puis sortie structurée), moins coûteuses et vérifiables ; les contrôles d'un relevé sont déterministes (`checkStatement` du domaine), le modèle explique et ne peut ajouter une anomalie qu'appuyée sur des lignes du relevé. Échec, mode manuel ou plafond : la conversation est escaladée à l'équipe avec un message d'attente au client |
| 2026-09-26 | File d'approbation : exécution une seule fois, refus motivé | `POST /admin/approvals/{id}/decide` (même chemin qu'à l'étape 12, servi par le module des agents) : décision par mise à jour conditionnelle (une approbation déjà décidée répond 409, deux décisions simultanées n'en exécutent qu'une), action exécutée avec la clé d'idempotence `approval-<id>` (remboursement par le module des paiements, crédit `goodwill` de référence unique, décision sur un document), résultat ou erreur gardés ; une exécution en échec peut être relancée en approuvant de nouveau, sans double effet. Refus : motif obligatoire (3 caractères au moins) ; le refus d'une action proposée à un client remet la conversation à l'équipe. Une anomalie comptable approuvée est confirmée : l'ajustement du relevé reste une décision de la comptabilité. Données d'une action revalidées avant exécution (une ligne mal formée donne une erreur d'exécution, jamais un effet partiel) |
| 2026-09-26 | Outils internes : chaque appel est une exécution de l'agent, dans son mode et ses plafonds | `/v1/internal/tools/*` (14 routes) : clé de service rattachée à un agent (portée `tools:*`), ou personnel (administrateur, opérateur) et rôle `agent` avec `agentCode`. Un outil non déclaré par l'agent est refusé ; `subjectUserId` borne les outils du client (une course d'un autre client est introuvable) ; remboursement et crédit plafonnés à 5 000 cents (réglages `agents.max_refund_cents`, `agents.max_credit_cents`, refus au-delà avec consigne d'escalade), vérifiés avant le reste remboursable ; en mode `approval` ils deviennent des approbations, en `auto` ils s'exécutent jusqu'au seuil `maxAutoRefundCents` de l'agent. Recrutement : `proposeDecision` crée toujours une approbation et le mode `auto` est refusé (409 `AGENT_MODE_LOCKED`), validation humaine obligatoire en V1 |
| 2026-09-26 | Minimisation et contenus des utilisateurs | Textes des clients et contenus des documents transmis au modèle dans une balise `<donnees_utilisateur>` (balises internes neutralisées), numéros de carte (Luhn) et NAS masqués avant envoi et journalisation ; jamais de téléphone ni de courriel dans les outils ; numéro d'un document réduit à ses 4 derniers caractères pour le modèle de décision. Seule l'extraction (vision) reçoit le document lui-même ; les champs lus sont gardés dans `driver_documents.extracted_fields` pour le contrôle humain |
| 2026-09-26 | Conversations, déclencheurs et rapports | Migration 0013 : `conversations` (une par client et par canal tant qu'elle n'est pas fermée ; téléphone gardé seulement pour répondre à un client sans compte) et `conversation_messages` (identifiant externe unique : un message reçu deux fois n'est traité qu'une fois), `agent_prompts` (prompts versionnés `docs/agents/<agent>.v<n>.md`, chargés par les données de départ, jamais réécrits). Réponse par le canal d'entrée (WhatsApp, push de l'application, écran web, texto), gabarit `agent.reply`. Événements `conversation.inbound`, `driver.document_uploaded` (ajouté, émis au téléversement) et `statement.issued` mis en file `agents` avec un identifiant déterministe (API et worker reçoivent l'événement avec Redis) ; en test, déclencheurs coupés sauf `AGENT_TRIGGERS=on`. Rapports : passe toutes les 5 minutes, quotidien dû à partir de 7 h et hebdomadaire le lundi (heure de Montréal, réglages `agents.report_hour`, `agents.report_weekly_weekday`), une fois par période ; courriel `agent.report` aux adresses de `agents.report_recipients`, sinon aux administrateurs. L'instantané `packages/api-client/src/openapi.json` n'est pas régénéré dans cette branche (en retard depuis l'étape 8) : à régénérer une fois les branches de l'étape 13 fusionnées |
| 2026-09-26 | Étape 13 en deux branches parallèles | Notifications, messagerie masquée, WhatsApp entrant et agent vocal dans `etape-13-notifications` ; infrastructure et agents IA dans `etape-13b-agents` (sous-agent), reliés par l'événement `conversation.inbound` (message entrant d'un client hors de l'application : WhatsApp, texto, voix, web) auquel l'agent relation client s'abonne |
| 2026-09-26 | Twilio pour les textos (D50), Resend, Expo, WhatsApp Cloud : adaptateurs REST sans SDK | Twilio : textos, webhook de statut et textos entrants signés (`X-Twilio-Signature`, HMAC-SHA1 de l'adresse et des paramètres triés, vérifié sur l'exemple publié par Twilio). Resend : pièces jointes, clé d'idempotence, expéditeur `EMAIL_FROM`. Expo : lots de 100, tickets puis reçus, appareil désinstallé retiré. WhatsApp Cloud : messages de session et gabarits, abonnement (jeton de vérification) et signature `X-Hub-Signature-256` (nouvelle variable `WHATSAPP_APP_SECRET`). Telnyx et Brevo restent non livrés. Clés gardées en champs privés, absentes des journaux |
| 2026-09-26 | Matrice 5.14 en données, appliquée par l'outbox | `packages/domain/src/notifications/matrix.ts` : gabarit, destinataire, canaux, critique. L'outbox crée une ligne par canal de la matrice quand l'appelant n'impose pas de canal (compte) ; un tiers sans compte reçoit un texto ; WhatsApp remplacera le push pour un client qui a réservé par WhatsApp. Les gabarits (FR-CA et EN) sont rendus à l'envoi, dans la langue du destinataire ; un gabarit inconnu donne un texte générique |
| 2026-09-26 | Envoi : réservation atomique, repli texto des événements critiques | Chaque ligne `notifications` est réservée par un seul processus (identifiant provisoire `claim:`), envoyée, puis marquée envoyée ou en erreur ; une réservation abandonnée est libérée après 10 minutes. Attribution, arrivée, approche, SOS, suspension pour solde : si le push ne part pas (aucun appareil, refus), un texto le remplace, noté dans la ligne. Le courriel d'une facture ou d'un relevé attend son PDF 10 minutes au plus, puis part avec le lien seul. File `notifications` (worker avec Redis, API sans Redis, rien d'automatique en test), priorité aux événements critiques, reprise toutes les 30 secondes, reçus push tous les quarts d'heure |
| 2026-09-26 | Préférences : consentement marketing seulement en V1 | Les notifications de service (courses, paiements, relevés) ne se désactivent pas ; une notification marquée marketing exige le consentement `marketing` en vigueur (Loi 25). Aucune communication marketing n'est envoyée en V1 : le lien de désabonnement viendra avec la première campagne |
| 2026-09-26 | Messagerie masquée : relais par texto, pas d'appel masqué en V1 | Un client sans application (réservation par téléphone) reçoit le texte du chauffeur par texto depuis le numéro de Neomoov et répond au même numéro : la réponse est ajoutée à la course (canal `sms`) et relayée au chauffeur ; aucun des deux ne voit le numéro de l'autre. Appels masqués : pas de relais vocal en V1 (Twilio Proxy est retiré, un relais maison demande un numéro et un gabarit d'appel dédiés) ; la messagerie de l'application et le relais par texto couvrent le besoin, l'exploitation reste joignable |
| 2026-09-26 | « Chauffeur en approche » : 700 m du départ, une seule fois | Évalué à chaque position d'un chauffeur en route (point de départ gardé en mémoire, relu chaque minute tant que la course n'est pas en route), envoi unique garanti en base (verrou par course et signal `driver_approaching` du journal). Seuil `notifications.approach_meters` |
| 2026-09-26 | Agent vocal : outils de réservation sans création de compte | `POST /v1/webhooks/vapi` (secret `x-vapi-secret`) : `quote`, `createRide`, `rideStatus`, `cancelRide`, `transferToHuman`. Appelant reconnu par son numéro (compte client, sa langue, ses crédits) ; sinon fiche minimale payée au chauffeur, comme une réservation par l'exploitation : aucun compte n'est créé sans consentement (Loi 25). Numéro masqué : transfert. Texto de confirmation. Journal des appels dans `agent_runs` (`voice_call_center`), numéro masqué. Configuration de l'assistant : `docs/voice-agent.md` |
| 2026-09-26 | SOS : appel du fondateur | En plus du push et du texto à l'exploitation, l'agent vocal appelle le fondateur (`alerts.founder_phone`, assistant `voice.sos_assistant_id`), une fois par incident ; sans ces réglages, aucun appel n'est tenté |
| 2026-09-26 | Conversations par texto : canal `sms` | Un texto entrant sans course en cours est confié à l'agent relation client comme un message WhatsApp : la conversation porte le canal `sms` (ajouté à la contrainte de la migration 0013 avant sa fusion, appliqué à la base de développement) et la réponse repart par texto au même numéro |
| 2026-09-26 | Assistance dans l'application et sur le web : routes ajoutées par le sous-agent, retenues | `POST /v1/me/support/messages` et `GET /v1/me/support/conversation` émettent `conversation.inbound` (canaux `app` et `web`), limités à `agents.client_messages_per_hour` (30) par client ; les écrans mobiles et web viendront avec la finition des applications |
| 2026-09-26 | Observabilité (étape 15, tâche 6) : Sentry par plateforme, chargé seulement avec un DSN | API et worker : `@sentry/node` 11.0.0, chargé par import dynamique seulement si `SENTRY_DSN` est renseignée (sinon ni code chargé ni appel réseau), `initWithoutDefaultIntegrations` avec les seules intégrations d'erreurs (filtres, erreurs liées, doublons, exceptions et promesses non rattrapées, contexte Node) : pas d'instrumentation HTTP (les adresses sortantes portent des clés), pas de traces, `dataCollection` tout à faux (utilisateur, témoins, en-têtes, corps, paramètres d'URL, SQL, données des tâches, variables locales ; `sendDefaultPii` n'existe plus en v11). Web : `@sentry/nextjs` 11.0.0 sans `withSentryConfig` (pas de greffon de construction ni d'envoi des cartes de code source tant qu'aucun jeton n'existe), initialisé dans `instrumentation-client.ts` et `instrumentation.ts` (`onRequestError`) par import dynamique : le SDK est un morceau à part, chargé seulement avec `NEXT_PUBLIC_SENTRY_DSN` ; l'origine d'ingestion du DSN est ajoutée à `connect-src` de la CSP. Mobiles : `@sentry/react-native` 7.11.0, la version que recommande Expo SDK 57 (`bundledNativeModules`), plutôt que la 8.28 : sans greffon Expo pour l'instant (pas d'envoi des cartes ni des symboles natifs, donc aucun jeton exigé par EAS) ; `sendDefaultPii`, capture d'écran et arbre des vues désactivés ; le module natif est lié automatiquement (nouveau build natif requis, sans effet tant que `EXPO_PUBLIC_SENTRY_DSN` est vide). Les deux bundles JavaScript (client Android, chauffeur iOS) et la construction Next.js ont été vérifiés. `@sentry/cli` : script d'installation refusé dans `allowBuilds` (binaire fourni par ses paquets optionnels) |
| 2026-09-26 | Filtrage commun des données avant envoi | `packages/domain/src/privacy/redaction.ts` : une liste de clés secrètes et personnelles (téléphone, courriel, adresses, coordonnées, nom, IP) partagée par le journal pino (masquées désormais au premier et au deuxième niveau, alors que seules les clés secrètes de deuxième niveau l'étaient ; `code` et `key` restent lisibles) et par le crochet `beforeSend` et `beforeBreadcrumb` de Sentry sur les cinq applications ; textes libres nettoyés par motifs (JWT, `Bearer`, clés `sk_`/`rk_`/`whsec_`/`nmk_`, paramètres `key`, `token`, `code`… des adresses, courriels, téléphones, codes postaux, adresses postales françaises et anglaises). Piles d'appels et métadonnées du SDK non modifiées ; utilisateur réduit à son identifiant |
| 2026-09-26 | Identifiant de corrélation propagé | Web et mobiles : `packages/api-client` envoie `X-Correlation-Id` (UUID, ou identifiant fourni par l'appelant), le même pour la nouvelle tentative après rafraîchissement du jeton. API : repris s'il fait 8 à 64 caractères sûrs, sinon créé ; AsyncLocalStorage ; une seule fois par ligne de journal (le `customProps` de pino-http, qui le doublait, est retiré). Files : `QueueService.add` pose `_correlationId` dans les données de la tâche (objets seulement, identifiant existant gardé ; une tâche planifiée reçoit un nouvel identifiant) et chaque traitement, BullMQ ou mémoire, s'exécute dans ce contexte. Événements de domaine : l'identifiant traverse Redis dans l'enveloppe. Suivi des erreurs : 500 et erreurs inattendues de l'API (pas les 502 et 503 voulus du mode dégradé), dernière tentative d'une tâche, abonné d'événement en échec ; web et mobiles : pannes 5xx de l'API vues par les requêtes, avec `correlationId` |
| 2026-09-26 | Métriques : définitions, fenêtres, Prometheus | `GET /v1/admin/metrics` (tout le personnel) et page My Hub « Métriques » (groupe Pilotage). Attribution et première offre : depuis la demande pour une course immédiate, depuis le début de la répartition (`state_timestamps.offering`) pour une planifiée ; 24 h ; première offre = plus ancienne `ride_offers.created_at` après ce début. États en cours comptés tels quels, états finaux sur 24 h. Latences : histogramme en mémoire par motif de route (seaux de 5 ms à 10 s, centiles interpolés comme Prometheus), fenêtre glissante de 15 minutes, par instance. Mesures en base gardées 15 s par instance. `GET /v1/internal/metrics` (texte Prometheus) : clé de service à nouvelle portée `metrics:read`. Disjoncteurs : compteurs cumulés ajoutés (échecs, ouvertures, dernier échec) sans changer `snapshot()`. À grande échelle, les lectures sur 24 h de `rides` et `notifications` parcourent la table : index d'expression ou table de cumul à ajouter si la page ralentit (aucune migration dans cette branche). Cibles de la section 2.2 dans `PERFORMANCE_TARGETS` (affichage seulement) |
| 2026-09-26 | Santé et battement du worker | `GET /v1/health` ajoute `environment` ; `version` lit `APP_VERSION` avant la version du paquet (à poser par le déploiement). Nouvelle variable `BETTERSTACK_HEARTBEAT_URL` : le battement du worker (chaque minute, par Redis) appelle le moniteur « heartbeat » de Better Stack, délai de 5 s, jamais bloquant. Sondes, seuils et alertes à créer : `docs/runbooks/observabilite.md` (aucun compte ouvert) |
| 2026-09-26 | `@opentelemetry/api` ajouté à `@neomoov/db` | `@sentry/node` apporte `@opentelemetry/api`, pair facultatif de `drizzle-orm` : pnpm résolvait alors deux copies de drizzle (API avec, `db` sans), dont les types ne se mélangent pas. Dépendance explicite dans `packages/db` et `pnpm dedupe` : une seule copie. Aucun traceur n'est enregistré, drizzle n'émet donc rien |
