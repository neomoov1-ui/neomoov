# Journal des décisions techniques

Une entrée par décision : date, contexte, décision, conséquences. Le cahier des charges fait foi ; ce journal couvre ce sur quoi il est muet.

| Date | Décision | Contexte et conséquences |
|---|---|---|
| 2026-09-22 | Dépôt créé, étape 0 | Cahier des charges v1.0, 18 prompts et design (lot 1, 20 maquettes) copiés dans `docs/`. Docker, pnpm et l'accès GitHub restent à installer sur le poste avant l'étape 1 |
| 2026-09-22 | Taxes des chauffeurs : pas de perception ni de remise en leur nom | Décision du fondateur (D26 du document de référence), conforme à la section 5.8 : les taxes sur les tarifs sont reversées au chauffeur, qui déclare lui-même. À faire confirmer par un comptable |
| 2026-09-22 | Dénomination légale | « GROUPE NOUVEAU SYSTEME KARDINAL (GROUPE NSK) INC. » pour Stripe, les magasins et les documents juridiques (D25) |
| 2026-09-22 | Poste de développement | pnpm 12.5.1 installé. Poste à 6 Go de mémoire vive, WSL absent : choix à faire entre Docker Desktop et une base de développement hébergée avant l’étape 1 |
| 2026-09-22 | Monorepo amorcé avant l’étape 1 complète | pnpm 12.5.1, Turborepo, TypeScript strict, `packages/config` et `packages/domain`. Les applications (API, worker, web, mobiles) restent à créer à l’étape 1, qui attend le choix entre Docker et une base hébergée |
| 2026-09-22 | Moteur de tarification écrit en avance (étape 4, partie pure) | `packages/domain/src/pricing`, 70 tests, couverture de 100 %. Aucune valeur métier dans le code : les règles sont des entrées |
| 2026-09-22 | Flex et Priorité ne se cumulent pas | Le cahier des charges est muet. Les deux options se contredisent (attendre jusqu’à 15 minutes, ou le chauffeur le plus proche tout de suite). Erreur `FLEX_AND_PRIORITY_EXCLUSIVE`. À confirmer par le fondateur |
| 2026-09-22 | Heures de pointe : donnée de configuration, plages provisoires | La section 5.1 ne les définit pas. Elles viennent de `settings` (`peakWindows`). Proposition provisoire, acceptée « on y reviendra » par le fondateur : du lundi au vendredi de 6 h 30 à 9 h 30 et de 15 h 30 à 18 h 30, plus les vendredis et samedis de 22 h à 2 h. Elles ne servent qu’à refuser l’Offre Flex, jamais à majorer un prix |
| 2026-09-22 | Forfait : options et suppléments ignorés, et signalés | Le forfait remplace tout le calcul (section 5.1). Le devis renvoie `ignoredOptions`. Un forfait dont le total ne se décompose pas en lignes arrondies est refusé (`FLAT_RATE_NOT_DECOMPOSABLE`) : My Hub devra valider le montant à la saisie |
| 2026-09-22 | Course offerte : Neomoov absorbe aussi les frais et les taxes | Confirmé par le fondateur. Une course « offerte » coûte 0,00 $ au client : le drapeau `waivesFees` est vrai pour les promotions « troisième course offerte » et « dixième course offerte » dans les données de départ. Le chauffeur reçoit son tarif normal en compensation |
| 2026-09-22 | Prix maximal consenti = prix affiché + marge de 20,00 $ au plus | Confirmé par le fondateur. « Plafond 20 $ » du prompt 04 : marge d’attente et d’arrêts (`maxExtraAllowanceCents`), l’attente facturée par minute entière après 5 minutes gratuites, jamais au-delà |
| 2026-09-22 | Ordre des opérations | Minimum, puis suppléments fixes, puis multiplicateur Flex ou Priorité, puis chauffeur favori, comme l’ordre des lignes de la section 5.1 |
| 2026-09-22 | Moteur de règlement écrit en avance (étape 9, partie pure) | `packages/domain/src/settlement` : `classifyRideForStatement`, `packBillingLines`, `buildStatement`, `periodForGeneration`, `evaluateSuspension`. Jeu de 200 courses mixtes recalculé indépendamment dans le test |
| 2026-09-22 | Taxes du chauffeur quand une promotion réduit le prix payé | Le chauffeur reçoit les taxes calculées sur son tarif complet (compensation comprise), puisqu’il les remet lui-même (D26) ; Neomoov absorbe l’écart avec les taxes réellement perçues du client. Sur une course offerte à 0,00 $, il reçoit donc son tarif et ses taxes en entier. **À faire confirmer par le comptable** avec la question D2 (traitement fiscal de la compensation et de la redevance) |
| 2026-09-22 | Période du relevé en dates locales | Du lundi au dimanche en dates de Montréal, sans arithmétique de fuseau : une course appartient au relevé de sa date locale. Générée le vendredi, la période est la dernière semaine entièrement écoulée |
| 2026-09-22 | Base de développement hébergée (option B) plutôt que Docker sur le poste | Décision du fondateur : le poste n’a que 6 Go de mémoire vive et WSL n’est pas installé. PostgreSQL et PostGIS chez Supabase, région `ca-central-1` (Canada central), compte Neomoov ; Redis chez Railway (aucune région canadienne chez Railway ni Upstash, acceptable pour la V1 : la migration canadienne avant le lancement commercial est déjà prévue, section 10.3). Docker reste utilisé seulement dans l’intégration continue GitHub. L’étape 1 est adaptée : `pnpm db:up` devient facultatif, `.env` porte `DATABASE_URL` et `REDIS_URL` hébergées. Connexion à Supabase par le « session pooler » (IPv4), jamais la connexion directe (IPv6 seulement) |
| 2026-09-22 | Schéma de données dans `packages/db` plutôt que dans `apps/api/src/db` | L'API et le worker partagent le même schéma : un paquet évite un import croisé entre applications. Drizzle ORM 0.45, `postgres` 3.4, migrations dans `packages/db/drizzle`, scripts `pnpm db:generate`, `db:migrate`, `db:rollback`, `db:reset`, `db:seed` à la racine. Le prompt 02 s'applique avec ce chemin |
| 2026-09-22 | Migrations inverses écrites à la main | drizzle-kit ne génère pas de migration inverse : chaque `drizzle/NNNN_nom.sql` a son inverse dans `drizzle/down/NNNN_nom.sql` ; `db:rollback` l'exécute et retire l'entrée du journal `drizzle.__drizzle_migrations`. Un test vérifie que chaque migration a son inverse |
| 2026-09-22 | Types PostGIS : correction du SQL généré | drizzle-kit met les types paramétrés entre guillemets (`"geography(point,4326)"`), ce que PostgreSQL refuse. `scripts/fix-migration.js` retire ces guillemets après chaque génération (chaîné dans `db:generate`). Colonnes : `geography(point,4326)` pour les positions, `geography(polygon,4326)` pour les zones, `geography(linestring,4326)` pour les traces, index GiST |
| 2026-09-22 | `driver_locations` partitionnée par jour, `driver_presence` pour la position courante | La table de Drizzle est remplacée en migration 0001 par une table partitionnée (`ensure_driver_locations_partition`, `purge_driver_locations` à 90 jours). La recherche « chauffeurs dans un rayon » ne lit pas l'historique : elle lit `driver_presence` (une ligne par chauffeur en ligne, index GiST) via la fonction `drivers_within(lng, lat, rayon, catégorie)` |
| 2026-09-22 | Numérotation sans trou par table de compteurs | Les séquences PostgreSQL laissent des trous en cas d'annulation de transaction. Table `counters` (portée, dernière valeur) avec `next_counter(portée)` transactionnel : `next_invoice_number()` (NM-0000001), `next_invoice_supplier_sequence(chauffeur)` (séquence par fournisseur, exigée pour la facturation), `next_ride_public_number(fuseau)` (NM-AAAA-MM-JJ-0001), `next_driver_public_number()` (CH-00001) |
| 2026-09-22 | État de course `expired` ajouté | La section 5.2 mentionne l'« expiration » de l'état `quoted` sans état cible. `expired` est l'état terminal d'un devis jamais confirmé ; il n'apparaît dans aucune liste client. Machine à états déclarée dans `packages/domain/src/rides/state-machine.ts` avec gardes nommées (`vehicle_category_at_least_reserved`, `waited_five_minutes_and_two_contacts`) et effets déclarés, sans logique d'infrastructure |
| 2026-09-22 | Tables en ajout seul par déclencheur | `audit_log` et `ride_events` refusent UPDATE et DELETE (`forbid_change()`, code `insufficient_privilege`). Les purges de rétention (section 5.15) devront passer par un rôle qui désactive le déclencheur, jamais par l'API |
| 2026-09-22 | Réglages en base, lus à chaque calcul | `settings` (clé, portée, valeur JSON) porte tous les nombres d'exploitation (frais 200, redevance 90, TPS 50 000 ppm, TVQ 99 750 ppm, annulation 120 s et 500, non-présentation 700, attente 300 s et 50 par minute, rayons 2, 5, 10 km puis zone, vagues 20 s, offres 15 s, seuil de solde 15 000, impayé 7 jours, heures de pointe D29). `buildPricingRules` (`packages/db`) assemble les `PricingRules` du moteur à partir des tables ; un test vérifie que les données de départ redonnent l'exemple de contrôle (24,55 $ et 31,56 $) |
| 2026-09-23 | Retours du fondateur : décisions D31 à D47 du document de référence v1.1 | Cahier des charges v1.1 copié dans `docs/`, amendements aux prompts dans `docs/prompts/18-amendements-v1-1.md` (à lire avec chaque prompt). Points qui touchent le code : préavis de 2 heures (`rides.min_lead_seconds` 7200, drapeau `FEATURE_IMMEDIATE_RIDES` désactivé, erreur `LEAD_TIME_TOO_SHORT`), péages et trafic à l'heure prévue dans le devis, `benchmarkCheck` contre `competitor_benchmarks`, négociation bornée à 70 % avec contre-offre au-dessus du prix affiché derrière `FEATURE_NEGOTIATION_ABOVE_MAX` (avis juridique en attente), deux choix de paiement (`prepaid`, `pay_driver_after`) et adaptateur `InstallmentProvider` (plus de 150 $), préférences étendues, `client_driver_links` (« Mes chauffeurs », « Mes clients »), `organizations` et `organization_id` dès le schéma, entretien vocal des candidats (V1.1), enregistrement audio et vidéo (V2), photos réelles uniquement dans les écrans (`docs/design/credits-photos-site.json` pour les crédits du site). La cinquième entité s'appelle provisoirement Kardinal Labs (D31) |
| 2026-09-23 | Réglages de départ à ajouter au prochain passage sur `packages/db` | `rides.min_lead_seconds` 7200, `pricing.negotiation_floor_ppm` 700000, `pricing.negotiation_ceiling_ppm` 1300000, `pricing.benchmark_margin_ppm` 50000, `payments.installment_min_cents` 15000 ; drapeaux `FEATURE_IMMEDIATE_RIDES`, `FEATURE_NEGOTIATION_ABOVE_MAX`, `FEATURE_INSTALLMENTS`, `FEATURE_RIDE_SERIES` désactivés. Le réglage `rides.scheduled_min_lead_seconds` (1800) devient le préavis des courses immédiates quand le drapeau sera activé |
| 2026-09-24 | Serveurs chez LWS (D48) : VPS KVM, Docker Compose, Caddy ; base Supabase Canada central ; Railway et Vercel en secours seulement | Décision du fondateur. Sections 3.5, 10.1 à 10.3 du cahier des charges v1.1 et amendements des prompts 01 et 16. Tous les centres de données LWS sont en France : la base et les documents restent au Canada, la résidence des serveurs applicatifs est une décision à prendre avec l'avocat avant le lancement commercial (conserver LWS avec l'évaluation Loi 25, ou rapatrier au Canada ; `compose.prod.yml` doit fonctionner à l'identique sur les deux) |
| 2026-09-24 | Base de développement Supabase `neomoov-dev` migrée et semée | Migrations 0000 et 0001 appliquées (72 tables, PostGIS 3.3.7, pgcrypto), données de départ créées. Les scripts de `packages/db` chargent le `.env` de la racine (`src/env.ts`) et exigent TLS hors localhost. `TEST_DATABASE_URL` reste vide : les tests d'intégration attendront un projet de test distinct |
| 2026-09-24 | Étape 1 : versions et outils | Versions épinglées : NestJS 12.1, Next.js 16.3 (App Router, Tailwind 4.3, TanStack Query 5, zustand 5), Expo SDK 57 (React Native 0.86, React 19.2, Expo Router 57, Reanimated 4), i18next 26 et react-i18next 17, Drizzle 0.45, vitest 5, TypeScript 7.0.2, Turborepo 2.11, pnpm 12.5.1, Node 24. pnpm en disposition isolée (défaut) : le réglage `node-linker=hoisted` écrit la veille n'était pas en vigueur (`node_modules/.modules.yaml` : `isolated`) alors que tout passait, il est retiré pour que l'intégration continue reproduise exactement le poste ; à revoir seulement si une compilation Expo échoue sur une double copie de react. Scripts d'installation autorisés dans `pnpm-workspace.yaml` (`allowBuilds`) ; NestJS 12 et dotenv 18 exclus de la politique d'âge minimal. Les tests NestJS passent par le greffon swc de vitest (métadonnées de décorateurs), la construction par tsc. `pnpm dev` lance API, worker et web ; les mobiles par `pnpm dev:client` et `pnpm dev:driver` |
| 2026-09-24 | Files et cache sans Redis | Sans `REDIS_URL`, l'API et le worker exécutent les tâches en mémoire, immédiatement et sans persistance, avec un avertissement au démarrage ; `GET /v1/health` rapporte `redis: not_configured` et `queues.mode: memory`. En production, `REDIS_URL` est obligatoire (refus au démarrage). Redis tournera sur le VPS LWS |
| 2026-09-24 | Valeur vide = variable absente | Un `.env` copié de `.env.example` contient `CLE=` sans valeur ; Zod refusait alors les énumérations (`PAYMENT_PROVIDER` vide) et l'API ne démarrait pas. `loadEnv` retire les valeurs vides avant validation : les défauts s'appliquent, `DATABASE_URL=` reste refusée. Test ajouté |
| 2026-09-24 | Client d'API écrit à la main à l'étape 1, génération typée reportée à l'étape 10 | `packages/api-client` : `createApiClient` sur `fetch` (préfixe `/v1`, `Accept-Language`, jeton porteur, identifiant de corrélation, délai maximal, clé d'idempotence), rejeu unique après rafraîchissement du jeton sur 401, `ApiError` au format `{ code, message, details, correlationId }` avec `status` 0 pour `NETWORK_ERROR`, `TIMEOUT` et `ABORTED` ; 14 tests sur un `fetch` simulé. La description OpenAPI est exportée sans démarrer de serveur (`pnpm openapi`, script `openapi:export` de l'API via `getOpenApiDocument`) dans `src/openapi.json`, commitée. Les types par ressource (openapi-typescript) viendront à l'étape 10, quand les modules existeront |
| 2026-09-24 | Intégration continue et Docker Compose | `.github/workflows/ci.yml` : services PostGIS 16-3.4 et Redis 7, `pnpm install --frozen-lockfile`, migrations et données de départ, lint, typecheck, tests, build, cache pnpm et Turborepo. `infra/docker-compose.yml` (PostGIS, Redis, Mailpit, MinIO) sert à l'intégration continue et aux postes qui ont Docker ; le poste actuel a la ligne de commande Docker sans moteur en marche, les commandes de développement n'en dépendent pas. Sorties de test retirées du cache Turborepo (avertissements « no output files ») |
| 2026-09-24 | Variables ajoutées pour les fournisseurs de repli | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` (textos et numéro vocal, compte Twilio existant recommandé au fondateur plutôt que Telnyx), `BREVO_API_KEY` (courriels, compte existant), `VAPI_PHONE_NUMBER_ID`, `BETTERSTACK_TOKEN`. `.env.example` réécrit avec toutes les variables du schéma, groupées et commentées. Proposé au fondateur le 24 septembre et accepté le soir même (D49) : stockage des documents sur Supabase Storage (protocole S3, région Canada, variables `S3_*`) plutôt que Cloudflare R2, qui n'a aucune région canadienne (D48 : les documents restent au Canada) |
| 2026-09-24 | D49 et D50, décisions du fondateur : Supabase Storage, nouveau compte Twilio | **D49** : les documents des chauffeurs (pièces d'identité, permis, assurances ; plus tard enregistrements audio et vidéo) vont dans Supabase Storage, bucket privé `documents` du projet Supabase, accès par le protocole S3 (`S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`) ; Cloudflare R2 abandonné, les variables `R2_*` restent tolérées par le schéma mais ne sont plus documentées comme cible. Adaptateur `StorageProvider` réel à écrire sur un client S3 compatible (étape 3). **D50** : textos et numéro vocal par Twilio, sur un **nouveau compte Twilio au nom de Neomoov** (`neomoov1@gmail.com`), distinct du compte de Taxi Sylvain ; Telnyx abandonné (`TELNYX_*` tolérées, non documentées). Le même numéro Twilio sera importé dans Vapi pour l'agent vocal. Adaptateur `SmsProvider` réel à écrire sur l'API Twilio (étape 13) |
| 2026-09-24 | Déploiement LWS : images construites sur le serveur, déclenchées par `git push lws main` | VPS KVM M livré (Debian ou Ubuntu avec Docker). Un dépôt Git nu `/opt/neomoov.git` sur le serveur reçoit les poussées du poste ; son crochet `post-receive` extrait dans `/opt/neomoov` et lance `infra/deploy.sh build` (construction des trois images, migrations une seule fois hors des instances, `up -d`, attente des deux instances saines, retour automatique à la version précédente). Aucun identifiant GitHub n'est nécessaire sur le serveur. La publication sur GHCR par GitHub Actions (`.github/workflows/images.yml`) existe en parallèle pour le mode `deploy.sh pull <sha>`, qui demande un jeton de lecture des paquets sur le serveur. Compose : Caddy (TLS Let's Encrypt, `api`, `hub`, `reserver`), API en deux réplicas (le nom `api` est résolu par le DNS de Docker), worker, web, Redis avec persistance ; limites mémoire ; `.env` de production hors Git avec secrets générés par `infra/server-setup.sh` |
| 2026-09-24 | Images Docker sans `pnpm deploy` | Trois étapes par image : installation filtrée complète et construction, installation filtrée `--prod`, image finale `node:24-alpine` qui reçoit les `node_modules` de production et les `dist` construits (plus `packages/db/drizzle` pour les migrations). `pnpm deploy` est évité : son comportement change entre les versions majeures de pnpm. Web en mode `standalone` de Next.js avec `outputFileTracingRoot` à la racine du monorepo ; `NEXT_PUBLIC_API_BASE_URL` est un argument de construction. `dotenv` devient une dépendance de production de `packages/db` (les scripts de migration s'exécutent dans l'image de l'API). `.gitattributes` force LF sur les scripts, Dockerfile et Caddyfile |
| 2026-09-25 | Étape 3 : politique d'accès par défaut « refus », déclarée route par route | Chaque route porte `@Public()`, `@Authenticated()`, `@Roles(...)` ou `@Scopes(...)` ; `assertRoutePolicies` (DiscoveryService et métadonnées NestJS) refuse le démarrage de l'API si une route n'en a aucune, et le test `authorization.e2e` parcourt chaque endpoint (401 sans jeton, 403 pour un rôle non admis, 403 pour un compte de service hors portée). Trois gardes globales dans l'ordre : limite par adresse IP, authentification et politique (rôles, portées, limite par utilisateur), propriété de la ressource (`@Owns('device' \| 'dataRequest' \| 'driverProfile' \| 'ride' \| 'user')`, le personnel voit tout, 404 si la ressource n'existe pas). Un `admin` est admis partout où un rôle du personnel l'est ; sinon les rôles sont explicites |
| 2026-09-25 | Rôles `finance` et `readonly` ajoutés à `user_role` (migration 0002) | Le prompt 03 les nomme pour le second facteur ; la section 4.1 ne les listait pas. `STAFF_ROLES` = admin, operator, finance, readonly. La migration inverse recrée le type sans ces valeurs (PostgreSQL ne retire pas une valeur d'énumération) |
| 2026-09-25 | Jetons : JWT HS256 de 15 minutes (`jose`), rafraîchissement opaque haché, rotation, familles, révocation immédiate | Jeton d'accès signé avec `JWT_ACCESS_SECRET` (`sub`, `sid`, `roles`, `amr`, `typ: access`) ; jeton de rafraîchissement `rt_…` aléatoire (32 octets), stocké en SHA-256 dans `sessions` avec une `family` : la rotation révoque la ligne et en crée une nouvelle dans la famille ; un jeton déjà tourné qui est représenté révoque toute la famille (`REFRESH_TOKEN_REUSED`), une rotation concurrente aussi. Révocation immédiate des jetons d'accès par un drapeau `revoked:<sid>` (Redis, ou mémoire sans Redis) vérifié à chaque requête, d'une durée égale à celle du jeton. Les jetons de passage (second facteur, liaison Apple ou Google) sont des JWT courts signés avec `JWT_REFRESH_SECRET`, à usage unique (`jti` consommé). Durées lues dans `settings` (`auth.*`) |
| 2026-09-25 | Codes SMS : 6 chiffres hachés en HMAC-SHA256 avec `ENCRYPTION_KEY`, un seul code actif par numéro | `otp_codes` ne contient jamais le code en clair ; un nouveau code consomme les précédents ; 5 minutes, 5 tentatives (puis code consommé), 30 s entre deux envois par numéro, 5 par numéro et 20 par adresse IP par heure (réglages `auth.otp_*`). Le code n'est écrit dans le journal qu'en `NODE_ENV=development` (champ `devOtpCode`) ; en test, le fournisseur simulé le conserve. Le texto est produit par `common/i18n.ts` (fr, en) |
| 2026-09-25 | Apple et Google : liaison à un téléphone vérifié, vérification des jetons par les JWKS des fournisseurs | `POST /auth/apple` et `/auth/google` vérifient le jeton d'identité (signature, émetteur, audience `APPLE_CLIENT_IDS` ou `GOOGLE_CLIENT_IDS`, nonce). Compte lié : session. Sinon `{ status: 'phone_required', linkToken }` : l'application demande un code SMS et renvoie `linkToken` à `otp/verify`, qui crée le compte (ou lie l'identifiant au compte du téléphone). Un identifiant déjà lié à un autre compte est refusé (`SOCIAL_ID_ALREADY_LINKED`). `SOCIAL_LOGIN_PROVIDER=mock` accepte `mock-apple:<sujet>:<courriel>` en développement et en test |
| 2026-09-25 | Personnel de My Hub : argon2id en WebAssembly, TOTP RFC 6238 maison, secret chiffré, verrouillage progressif, `amr` en session | Mots de passe argon2id (64 Mio, 3 itérations) par `hash-wasm` : pas de module natif à compiler dans les images Docker. TOTP (SHA-1, 30 s, 6 chiffres, tolérance d'un pas, rejeu interdit par drapeau) écrit dans `common/crypto.ts` sans dépendance ; QR en SVG par `qrcode`. Secret TOTP chiffré AES-256-GCM avec `ENCRYPTION_KEY` (`totp_secret_encrypted`), dix codes de secours hachés. Inscription obligatoire à la première connexion (`mfa_enrollment_required`), vérification ensuite (`mfa_required`). Cinq échecs : 15 minutes de verrou, doublées à chaque série (`auth.staff_lockout_*`) ; réponse identique pour un courriel inconnu (hachage factice vérifié). Colonne `sessions.amr` : les rôles du personnel ne sont accordés qu'aux sessions ouvertes avec `mfa`, y compris après rotation ; une connexion par code SMS d'un membre du personnel ne donne que ses rôles ordinaires, et `GET /me` montre les rôles de la session. Création du premier administrateur par `pnpm --filter @neomoov/api create-staff` (mot de passe par `STAFF_PASSWORD`), manuel `docs/runbooks/personnel-my-hub.md` |
| 2026-09-25 | Comptes de service : clés `nmk_<préfixe hex>_<secret>` à portée limitée (table `api_keys`) | Secret affiché une fois, stocké en SHA-256, recherche par préfixe, comparaison à temps constant, révocation et expiration, `last_used_at` au plus une fois par minute. Portées `agents:run`, `agents:read`, `tools:*` (joker), `rides:read`, … ; une route sans `@Scopes` est fermée aux clés, `@Scopes('*')` accepte toute clé valide. L'acteur d'audit d'une clé est son `agent_code` (ou `key:<nom>`) |
| 2026-09-25 | Journal d'audit : intercepteur global sur toute mutation, colonne `correlation_id`, masquage | Les services enregistrent leurs entrées (`AuditService.record`, avant et après) dans un contexte AsyncLocalStorage ouvert par l'intercepteur ; à la fin de la requête, elles sont écrites avec l'acteur, l'adresse IP et l'identifiant de corrélation. Une mutation sans entrée explicite reçoit une entrée générique `<méthode> <route>` ou celle du décorateur `@Audit`. Masquage récursif des clés sensibles (mot de passe, jeton, secret, code, haché, clé) et troncature des chaînes longues. L'échec de l'écriture est journalisé mais ne fait pas échouer la requête. Lecture par le personnel : `GET /admin/audit` (curseur temporel) |
| 2026-09-25 | Limitation de débit : compteurs à fenêtre fixe, Redis ou mémoire, seuils en base | `RateLimitService` (INCR et EXPIRE sur Redis, `Map` en mémoire sans Redis) : 300 requêtes par adresse IP et par minute avant l'authentification, 600 par utilisateur après (`ratelimit.*`), limites propres aux codes SMS et à la connexion du personnel (10 par courriel et 30 par adresse par 10 minutes), en-têtes `X-RateLimit-*` et `Retry-After`, code `RATE_LIMITED`. Sans Redis, chaque instance compte pour elle-même : en production Redis est obligatoire (déjà exigé) |
| 2026-09-25 | Loi 25 (5.15) dans l'API : consentements versionnés, demandes de droits, export JSON et PDF, suppression par anonymisation | Consentements : une ligne par accord, retrait horodaté, nouvelle version = nouvelle ligne (l'ancienne est close) ; `GET /me/consents` donne l'état par finalité. Politique de confidentialité : version en vigueur dans `settings` (`legal.privacy_policy_version`), exigée à la création du compte et acceptée par `PATCH /me` ; `GET /me` expose `privacyPolicyAccepted`. Demandes (`data_requests`, échéance `privacy.data_request_due_days` = 30 jours en date de Montréal) : accès et portabilité produisent, par la file `privacy`, un export JSON et un PDF (`pdfkit`) déposés dans le `StorageProvider` (`exports/<utilisateur>/<demande>.json|.pdf`), lien signé de 7 jours renvoyé par `GET /me/data-requests/{id}` et envoyé par courriel (sinon texto) ; retrait de consentement traité immédiatement ; la suppression passe par `DELETE /me`. Suppression : statut `deleted` et sessions révoquées tout de suite, puis tâche `account-deletion` : sessions, appareils, codes, identifiants du personnel, adresses et favoris effacés ; téléphone remplacé par `+999` et 11 chiffres dérivés de l'identifiant (le numéro redevient libre), courriel, noms et identifiants Apple ou Google effacés ; courses conservées avec leurs champs de passager effacés ; chauffeur passé à `offboarded`. Le compte du personnel ne se supprime pas depuis l'application. En mode mémoire (sans Redis), `PrivacyJobsService` traite la file dans le processus courant ; avec Redis, dans le worker |
| 2026-09-25 | OpenAPI produite à partir des schémas Zod | `z.toJSONSchema` (Zod 4, cible OpenAPI 3.0) alimente `@nestjs/swagger` par les décorateurs `ZodBody`, `ZodResponse`, `ZodQuery` et `ApiErrors` (réponses d'erreur au format unique) ; les schémas de l'identité vivent dans `packages/domain/src/schemas/auth.ts` (primitives déplacées dans `common.ts`). Le test d'autorisation vérifie que chaque route inventoriée figure dans le document |
| 2026-09-25 | Tests d'intégration sur la base de développement, comptes suivis par fichier | `TEST_DATABASE_URL` reste vide : les tests tournent sur `DATABASE_URL` (Supabase de développement), avec des téléphones `+1999…` et des courriels `@test.neomoov.local`. Chaque fichier de test tourne dans son processus et ne supprime à la fin que les comptes qu'il a créés (identifiants suivis), jamais par motif : la première version supprimait les comptes des autres fichiers en cours d'exécution. Le délai entre deux codes SMS est neutralisé dans l'aide de test en antidatant les codes précédents. Les lignes d'audit des tests restent (table en ajout seul) |
| 2026-09-25 | Revue de code de l'étape 3 : 15 constats corrigés | Le plus grave : `SOCIAL_LOGIN_PROVIDER` valait `mock` par défaut sans refus en production, ce qui aurait accepté des jetons forgés (`mock-apple:<sujet>`) et permis de prendre tout compte lié ; `loadEnv` exige maintenant `real` en production et `infra/server-setup.sh` l'écrit. Second facteur : le verrouillage est vérifié à chaque étape (TOTP, code de secours, confirmation) et le compteur d'échecs n'est remis à zéro qu'après le second facteur. Suppression de compte : si le worker n'a pas encore anonymisé le numéro, une reconnexion avec ce numéro le libère (`releasePhone`) avant de créer le nouveau compte ; toute violation d'unicité à la création devient 409 `ACCOUNT_CONFLICT`. Courriel remis par Apple ou Google : repris seulement s'il est vérifié par le fournisseur et libre. Audit : les entrées sont écrites avant l'envoi de la réponse, et celles enregistrées avant une exception sont conservées ; curseur de pagination sur (instant, identifiant) pour ne sauter aucune entrée partageant le même instant. Rafraîchissement : le compte est vérifié avant la rotation (plus de session orpheline pour un compte bloqué). Journal : `*.code` et `*.key` retirés du masquage (`err.code` doit rester lisible). `create-staff` met à jour un compte existant par courriel ou téléphone et affiche une erreur propre. Jeton de liaison Apple ou Google vérifié avant de consommer le code SMS, puis à usage unique. Code mort `OTP_LOCKED` remplacé : le cinquième échec renvoie 429 `OTP_LOCKED` et consomme le code. Seuils de la connexion du personnel et du jeton de liaison déplacés dans `settings` (`auth.staff_login_per_*`, `auth.link_token_ttl_seconds`) ; la durée de rejeu TOTP (90 s) reste une constante de protocole. Export PDF traduit (fr, en) et prénom échappé dans les courriels HTML ; `outcome` d'une demande est une note d'exploitation, l'utilisateur reçoit `status`. Appareils sans jeton push dédoublonnés par utilisateur et plateforme. Tests : les codes SMS de test sont retirés à la fin de chaque fichier |
| 2026-09-25 | Étape 4 : péages (D33) dans le moteur pur, ignorés sous un forfait | `QuoteInput.tollsCents` (montant réel de l'API Routes) devient une ligne `tolls` du prix affiché, ajoutée au sous-total avant taxes et jamais au tarif chauffeur. Un forfait est « tout compris » : les péages y sont ignorés (`tollsCents: 0`), comme les options. Un péage négatif est une entrée invalide |
| 2026-09-25 | Vérification concurrentielle (D33) : `benchmarkCheck` pure, plages horaires, référence la plus récente | `packages/domain/src/pricing/benchmark.ts` : six plages horaires de Montréal (`weekday_morning` 6 h à 10 h, `weekday_day` 10 h à 15 h, `weekday_evening` 15 h à 19 h, `weekday_night`, `weekend_day` 6 h à 19 h, `weekend_night`), référence = concurrent le moins cher du relevé le plus récent de moins de `pricing.benchmark_max_age_days` (14) pour la même catégorie, les mêmes zones (dans les deux sens) et la même plage. Si le total dépasse `référence × (1 − pricing.benchmark_margin_ppm)`, ligne `benchmark_alignment` négative qui réduit les frais de service jusqu'à 0 (jamais le tarif chauffeur, jamais la redevance), taxes et total recalculés, crédits plafonnés au nouveau total. `exceeded` reste vrai même sans marge de remise : événement `pricing.benchmark_exceeded` dans le journal d'audit et le journal applicatif (alerte quotidienne à l'étape 13). Table `competitor_benchmarks` (migration 0003) saisie par le personnel : `POST /admin/pricing/benchmarks`, liste, suppression ; aucune API d'Uber ou de Lyft n'est appelée |
| 2026-09-25 | Promotions : plafond de remise dans le moteur, traduction des lignes `promotions` par le service de devis | `Promotion.maxDiscountCents` plafonne une remise en pourcentage (LANCEMENT30 : 15,00 $). Le service traduit une ligne de la table : `nth_ride` → course offerte au n-ième trajet (`waivesFees` selon la ligne), `percent` → remise en points de base plafonnée, `free_ride` → course offerte ; `fixed` n'est pas admis sur un devis. Conditions vérifiées en base avant le moteur : promotion active et valide (`PROMO_CODE_UNKNOWN`), budget, limite globale et limite par client (`PROMOTION_NOT_APPLICABLE`) ; distance et rang de course par le moteur. Le nombre de courses terminées vient de `clients.ride_count`, les crédits de `credits.remaining_cents` non expirés |
| 2026-09-25 | Devis : préavis D32 appliqué au devis, toutes les catégories par défaut, persistance avec empreinte, mode dégradé | `POST /v1/quotes` sans `category` tarife toutes les catégories actives de la grille (trois en V1), même itinéraire, un enregistrement `quotes` par catégorie (empreinte SHA-256 des entrées, des lignes et de la version des règles ; validité `pricing.quote_validity_seconds`). `requestedAt` doit respecter `rides.min_lead_seconds` (7200) et `rides.max_lead_days` (30) : `LEAD_TIME_TOO_SHORT` avec `earliestPickupAt`, `LEAD_TIME_TOO_LONG` ; sans heure, course immédiate seulement si `FEATURE_IMMEDIATE_RIDES` est actif. Hors aire de service (aucune zone pour l'origine ni la destination) : `OUT_OF_SERVICE_AREA`. Itinéraire par `MapsProvider.route` avec `departureTime` = heure de prise en charge ; en cas d'échec (nouvelles tentatives et disjoncteur épuisés), estimation interne (vol d'oiseau × `pricing.degraded_distance_factor_bps`, vitesse `pricing.degraded_speed_kmh`), `estimated: true` sur la réponse et chaque devis, persisté. Les lignes renvoyées portent un `code` stable et un libellé de repli (fr, en) ; la somme des lignes vaut `amountDueCents`. `GET /v1/quotes/{id}` appartient au client du devis (`@Owns('quote')`) |
| 2026-09-25 | Zones en mémoire, temps d'arrivée provisoire, règles versionnées | `ZonesService` charge les polygones actifs (GeoJSON) et résout un point en JavaScript (lancer de rayon), cache de 5 minutes, priorité aéroport > centre-ville > quartier > aire de service ; PostGIS reste la source (`zones_containing` sert aux requêtes SQL). `PricingRulesService` assemble `buildPricingRules` depuis `settings`, `pricing_rules`, `surcharges` et `flat_rates` (jointure des codes de zones), cache 60 s, version = empreinte des sources (16 caractères) enregistrée sur chaque devis ; `POST /admin/pricing/rules/refresh` vide les caches. Temps d'arrivée : jusqu'à cinq chauffeurs `driver_presence` disponibles de la catégorie, actifs, dans `pricing.eta_search_radius_m`, puis `etaMatrix` ; sans chauffeur, `eta.status = on_availability` (« selon disponibilité ») |
| 2026-09-25 | Adaptateur Google Maps Platform réel (`GoogleMapsProvider`) | Routes API `computeRoutes` (`TRAFFIC_AWARE`, `departureTime` si dans le futur, `extraComputations: TOLLS`, péages en CAD convertis en cents, polyligne), `computeRouteMatrix` pour les temps d'arrivée (origine injoignable = une journée), Places nouvelle version (`places:autocomplete` avec jeton de session et biais de position, `places/{id}` avec `FieldMask`), Geocoding. Délai de 3 s par appel (`AbortSignal.timeout`), deux nouvelles tentatives sur erreur réseau, 408, 429 et 5xx (attente 200 ms puis 400 ms), aucune sur 4xx définitif (`MAPS_REQUEST_REJECTED`, 502), disjoncteur ouvert 30 s après cinq échecs consécutifs (`MAPS_UNAVAILABLE`, 503, sans appel réseau). Testé avec un `fetch` simulé ; la clé serveur n'est jamais exposée aux applications. `MapsProvider` gagne `placeDetails` et un point de proximité pour l'autocomplétion ; le simulateur peut échouer sur commande (`failRoutes`) pour tester le mode dégradé |
| 2026-09-25 | Amendements v1.1 au schéma appliqués (migration 0003) | Tables des amendements du prompt 02 qui manquaient : `competitor_benchmarks`, `organizations` (D42, ligne `neomoov` semée) avec `organization_id` nullable sur `drivers`, `vehicles`, `rides`, `weekly_statements` et `settings`, `client_driver_links` (D40) ; colonnes `payment_choice` (D36, `prepaid` ou `pay_driver_after`), `installment_provider_ref` et `tolls_cents` sur `rides` ; réglages du 23 septembre semés (`rides.min_lead_seconds` 7200, `pricing.negotiation_*`, `pricing.benchmark_margin_ppm`, `payments.installment_min_cents`) et drapeaux `immediate_rides`, `negotiation_above_max`, `installments`, `ride_series` désactivés. Reportés à leurs étapes : `ride_series` (prompt 05), `installment_plans` (07), `interview_results` et `recruitment_criteria` (13), `recording_consents` et `recordings` (14, V2) |
| 2026-09-25 | Schémas Zod des lieux et des devis déplacés dans `schemas/quotes.ts` | `coordinatesSchema`, `placeSchema`, `rideOptionsSchema`, `ridePreferencesSchema`, `quoteRequestSchema` (catégorie facultative), `quoteSchema` (péages, promotion, remise d'alignement, `amountDueCents`, `estimated`, `eta`), `quotesResponseSchema`, `quoteDetailSchema`, `simulateQuoteSchema`, schémas des lieux et des relevés. `index.ts` les réexporte ; aucun écran ne les consommait encore |
| 2026-09-25 | Revue de code de l'étape 4 : 15 constats corrigés | Détail du devis : la ligne « frais de service » affiche les frais avant remise d'alignement, la ligne négative `benchmark_alignment` porte la remise, et la somme des lignes vaut de nouveau le reste à payer (vérifié par le test D33). `POST /v1/quotes` ne renvoie plus les références concurrentielles ni la version des règles (réservées à la simulation My Hub). Une promotion réservée à certaines catégories n'est simplement pas appliquée aux autres quand toutes les catégories sont tarifées. Les relevés sont filtrés par catégories et plage horaire avant la limite de lecture. Une origine injoignable dans la matrice de temps vaut l'infini et n'est jamais affichée comme un temps d'arrivée. La ligne de forfait a le code stable `flat_rate` et `flatRateCode` vaut le code de la table `flat_rates` (`codeByCategory` dans les règles). Grilles, suppléments et forfaits filtrés sur `valid_from` (la ligne la plus récente déjà en vigueur gagne). Colonne `quotes.created_by_user_id` (migration 0003) : le demandeur sans profil client relit son devis. Aucun événement `pricing.benchmark_exceeded` pendant une simulation. Message du préavis calculé à partir du réglage. `ignoreLeadTime` couvre aussi le préavis maximal ; distance et durée forcées ensemble ou pas du tout. Temps d'arrivée calculés en parallèle et devis insérés en une seule instruction. `haversineMeters` et `pointInPolygon` dans `common/geo.ts`, partagées par le simulateur de cartes et le service. Le test du temps d'arrivée restaure le statut du chauffeur de démonstration. Le simulateur de cartes renvoie 404 `PLACE_NOT_FOUND` pour un lieu inconnu et n'appelle `geocode` qu'en l'absence de correspondance connue |
| 2026-09-25 | Étape 5 : l'état d'une course ne change que par la machine à états, sous verrou, avec idempotence | `RidesService.applyTransition` verrouille la ligne (`FOR UPDATE`), applique `transition()` du domaine, met à jour `state` et `state_timestamps` (horodatage de chaque état atteint) et écrit `ride_events` (type = événement, états avant et après, acteur `client`, `driver`, `operator`, `system` ou `agent`, données et effets déclarés). Rejouer la transition qui a produit l'état courant (dernier événement identique) renvoie la course sans nouvel événement ; toute autre transition impossible est un 409 `RIDE_INVALID_TRANSITION`. Aucune écriture directe de `state` ailleurs. L'attribution forcée depuis `requested` passe par `offers_sent` (système) puis `driver_accepts` (opérateur ou chauffeur), après vérification de la garantie modèle (`vehicle_categories.rank`) |
| 2026-09-25 | Création d'une course : devis du demandeur, clé d'idempotence en base, numéro par la fonction SQL | `POST /v1/rides` exige l'en-tête `Idempotency-Key` (8 à 80 caractères) : colonne `rides.idempotency_key` unique, la même clé renvoie la même course (200) ; le devis doit appartenir au demandeur, être valide, non déjà utilisé et porter le même prix maximal consenti ; le type suit `quotes.requested_at` (planifiée) et une course immédiate exige `FEATURE_IMMEDIATE_RIDES`. Numéro public par `next_ride_public_number(fuseau)`, montants copiés du devis, `created_by_user_id`, `organization_id` Neomoov. Création par l'opérateur (`POST /v1/admin/rides`) avec fiche minimale (`guest_name`, `guest_phone`, `guest_language`) ou compte client |
| 2026-09-25 | Frais d'annulation et non-présentation : fonctions pures, montants et délais en base | `packages/domain/src/rides/cancellation.ts` : `clientCancellationFeeCents` (0 avant l'attribution ou dans `rides.free_cancellation_seconds` après elle, sinon `rides.cancellation_fee_cents`), `noShowCheck` (au moins `rides.no_show_min_wait_seconds` sur place et `rides.no_show_min_contacts` tentatives : `POST /driver/rides/{id}/contact` et messages du chauffeur sur place comptent, colonne `contact_attempts`), `waitedSecondsBetween`. L'annulation par le chauffeur (`driver_cancels`) est suivie immédiatement de `reassign` (système) : la course redevient `requested` sans chauffeur, l'événement `ride.reassign_requested` est publié pour la répartition (étape 6), l'effet `driver_sanction` est journalisé en audit |
| 2026-09-25 | Prix final et trace de course | À `complete`, `finalizeQuote` du domaine ajoute l'attente (`waited_seconds`, mesurée entre `arrived` et `in_progress`) dans la limite du prix maximal consenti ; `final_price_cents`, `wait_charge_cents`, tarif et taxes mis à jour ; compteurs `drivers.ride_count`, `clients.ride_count` et lien `client_driver_links` (D40) alimentés ; événement `ride.completed` publié (paiement et facture aux étapes 7 et 9). Trace : positions `driver_locations` de la course (`ride_id` posé pendant les états actifs), simplifiées par Douglas-Peucker (10 m) dans `ride_tracks` avec distance et durée mesurées ; sans trace (moins de deux points), les mesures de l'application ou du devis servent |
| 2026-09-25 | Présence et positions : PostGIS source de vérité, Redis GEO en appoint, historique par lots | `driver_presence` (une ligne par chauffeur en ligne, disponible ou en pause) est mise à jour à chaque position ; `presence:geo` (Redis GEOADD) suit quand Redis existe. Passage en ligne : chauffeur actif, véhicule actif du chauffeur, documents `drivers.required_documents` approuvés et non expirés, pack actif si `drivers.require_active_pack`, aucune suspension ni solde bloquant ; refus 409 `DRIVER_NOT_ELIGIBLE` avec la liste des motifs ; position requise. Positions : rejetées (non comptées) si moins de `presence.min_distance_meters` et moins de 5 s après la précédente ; écrites par lots toutes les `presence.location_batch_ms` dans `driver_locations` (partition du jour créée par `ensure_driver_locations_partition`) ; présence expirée après `presence.expiry_seconds` sans position (balayage toutes les 30 s), quart `driver_shifts` fermé. Diffusion par l'événement `driver.location` : client de la course en cours seulement, et My Hub |
| 2026-09-25 | Temps réel : trois espaces Socket.IO, jeton à la connexion, salles, adaptateur Redis | `/client` (`ride.subscribe` après vérification que l'utilisateur participe à la course, `ride.updated`, `driver.location`, `message.received`), `/driver` (salle `driver:<id>` automatique, `status.update`, `location.update`, `ride.subscribe`), `/admin` (personnel seulement, salle `admin` : `ride.updated`, `driver.location`, `driver.presence`, `alert.new`). Jeton d'accès obligatoire dans `auth.token` (ou en-tête), sinon `error` puis déconnexion ; messages validés par Zod et acquittés `{ ok, … }`. Les événements de domaine (`DomainEventsService`, en mémoire) sont relayés par `RealtimeService` ; avec `REDIS_URL`, `RedisIoAdapter` partage salles et émissions entre les instances de l'API. Test : position chauffeur reçue par le client en moins de 2 secondes ; charge légère de 8 chauffeurs à 4 positions par seconde (200 chauffeurs pendant 2 minutes avec `LOAD_TEST=1`), aucune perte |
| 2026-09-25 | Réservation planifiée : signaux du worker, une fois chacun, et proposition volontaire des chauffeurs | `ScheduledService.tick(now)` (file `scheduling`, toutes les minutes dans le worker ; `POST /admin/rides/scheduled/tick` pour une passe manuelle) : rappel J-1 (`rides.scheduled_reminder_before_seconds`, pour les courses réservées plus de 24 h à l'avance), `scheduled.dispatch_due` à `rides.scheduled_assign_before_seconds` sans chauffeur (consommé à l'étape 6), alerte opérateur à `rides.scheduled_reassign_before_seconds` si rien n'est confirmé (`alert.new` My Hub, notification au personnel, `scheduled_assignments.operator_alerted_at`) ; chaque signal est un `ride_events` de type `scheduled_*`, jamais répété. `GET /driver/scheduled` liste les courses ouvertes de la catégorie du chauffeur (ou d'un rang inférieur) et les siennes ; `claim` crée une proposition (une seule active par course), `confirm` attribue par la machine à états, `decline` la retire. Les offres automatiques sur le créneau (D40) arrivent avec la répartition |
| 2026-09-25 | Messagerie masquée, partage, SOS, notifications en file | Messages dans `ride_messages` (client, chauffeur, opérateur ; fermés dans un état terminal), relayés en temps réel ; aucun numéro échangé (proxy vocal à l'étape 13). Partage : `tracking_token` (20 caractères) et `GET /v1/public/track/{token}` sans authentification : numéro de course, état, catégorie, destination, prénom et véhicule du chauffeur, sa position pendant les états actifs, expiré `rides.share_link_ttl_hours` après la fin. SOS : incident `type sos`, gravité `critical`, événement `ride.sos`, alerte `alert.new` My Hub, texto au personnel (file). Toutes les notifications de la section 5.14 sont mises en file dans `notifications` (gabarit, données, destinataire ou adresse pour un invité) ; l'envoi réel est branché à l'étape 13 |
| 2026-09-25 | Tests d'intégration des courses : nettoyage avec le déclencheur d'ajout seul suspendu | `ride_events` refuse toute suppression ; le nettoyage des comptes de test suspend le déclencheur dans une transaction (verrou exclusif bref) pour retirer les courses de test, puis le rétablit. Délai des tests d'API porté à 120 s (base distante, création de chauffeurs et de personnel avec argon2id) |
| 2026-09-25 | Revue de code de l'étape 5 : 15 constats corrigés | Détection du rejeu dans `applyTransition` limitée aux événements de transition (un message ou une position ne masque plus une transition). `assign` refuse (409 `RIDE_ALREADY_ASSIGNED`) une course active déjà attribuée à un autre chauffeur ; à la réattribution, l'ancien chauffeur quitte la salle de la course (`previousDriverId`). Frais d'annulation client calculés sous le verrou de la ligne. Une course d'invité n'a pas de compte client : notifications au téléphone de l'invité. Vue de course : véhicule pris par `rides.vehicle_id`, pas le véhicule courant du chauffeur. Authentification des sockets dans l'intergiciel d'espace (`server.use`) : refus en `connect_error` avec le code (`UNAUTHENTICATED`, `DRIVER_PROFILE_REQUIRED`, `FORBIDDEN_ROLE`) ; gardes HTTP et journal d'audit ignorés hors HTTP. Bus d'événements : origine par instance pour ignorer ses propres messages Redis, dates ISO restituées. Un seul adaptateur Socket.IO (`AppIoAdapter` : CORS de l'API, adaptateur Redis, fermeture des connexions à l'arrêt). Positions : intervalle minimal `presence.min_interval_seconds` et distance `presence.min_distance_meters` en base ; lot accumulé hors ligne par `POST /driver/locations` ; pendant une course active, une présence expirée ou retirée par un passage hors ligne est recréée en pause (le suivi du client continue, aucune nouvelle offre) ; `DRIVER_OFFLINE` sans course active ; `/driver/location(s)` hors du journal d'audit (`@NoAudit`). Planifiées : une seule requête `ride_events` par passe, proposition en double → 409. Index partiel `rides_quote_unique` (un devis, une course), migration 0004 regénérée avec son script de retour. Tests : refus de connexion lus dans `connect_error`, attribution attendue jusqu'à `assigned`, heure des simulations fixée à 14 h UTC (majoration de nuit de 23 h à 5 h, heure de Montréal), chaîne du seed corrigée. |
