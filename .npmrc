save-exact=true
auto-install-peers=true
# Disposition isolée de pnpm (défaut) : Metro (Expo SDK 57) suit les liens symboliques ; chaque application garde sa
# propre copie de react et react-native. À revoir seulement si une compilation Expo échoue sur une double copie de react.
